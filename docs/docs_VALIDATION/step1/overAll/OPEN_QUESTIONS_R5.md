# OPEN QUESTIONS / OVERNIGHT RULINGS — R5 Overall Validation

> Per ruling V-R5-8 (user asleep, 2026-06-11): every decision the manager took on the user's behalf, and every question deferred to the user, is logged here. Review in the morning; reverse anything you disagree with — each entry states what a reversal would cost.

## Manager rulings taken overnight

### OQ-R5-1 — V10 case-study matrix APPROVED as proposed — 2026-06-11
- **Decision:** approved the 12-cell matrix in `V10_matrix_proposal.md` verbatim, including the executor's two relocations: NYC rural moved from Catskill wilderness (2 buildings) to Woodstock NY (201 buildings, CZ 5A instead of 4A) and Austin rural moved from open Hill Country (0 buildings) to Marble Falls TX (184 buildings). LA rural (Lancaster, 143 @ 1500 m) kept as probed.
- **Why:** all 12 cells clear the ≥30-building threshold, every cell's EPW resolves, and the NYC-rural CZ shift to 5A widens the climate spread (2A/3B/4A/5A) rather than narrowing it — strictly better for a multi-climate case study.
- **Reversal cost:** low until V12 starts. Any cell can be swapped before its V12 run by editing its row and re-probing (~1 Overpass count query). After a cell has been simulated, swapping it costs one full cell re-run (Steps 1–3 local + one cluster array + Step 5), roughly 1–3 h wall-clock depending on building count.

### OQ-R5-2 — CP-V2 ruling: CBECS gates stay report-only; chain proceeds to Phase C — 2026-06-11
- **Decision:** accepted Phase B's diagnosis (V06–V08) and chose executor option 1+3: keep the CBECS NE gates report-only per standing ruling V-R5-5 and continue the chain (V09 → V11 → V12 → V13). No code change, no re-run. Consolidated memo written to `results/MEMO_phaseB_cbecs_diagnosis.md`.
- **Why:** the diagnosis falsified the fuel-basis hypothesis (correcting the basis worsens NMBE from −16.0% to −29.5/−35.3%) and showed the gap is fleet-composition + vintage-schedule mismatch — a comparison problem, not a model defect. Deep calibration (option 2) is research-scope, exactly what V-R5-5 says not to start tonight.
- **Reversal cost:** zero — nothing was changed. Option 2 can be opened as an R6 plan at any time; its prerequisites (PBA2-only Boston-climate CBECS sub-sample, vintage-matched schedules) are spelled out in the memo.

### OQ-R5-3 — Supermarket IDF manual Coil:Cooling:DX:SingleSpeed repair — 2026-06-11
- **Decision:** accepted the manual reconstruction of the `Coil:Cooling:DX:SingleSpeed` block in `refs_work_v23/Supermarket_V22.1.idf`. IDFVersionUpdater's two-pass chain (22.1→22.2→23.1) inserted 2017/2023 fan power split fields without correctly remapping subsequent fields, leaving `air_inlet_node_name` blank. The repair used the original v22.1 source as ground truth for field ordering; all non-fan-power values are byte-for-byte identical to the original.
- **Why:** the Supermarket reference sim is required for 1/31 of the reference EUI baseline. The automation-only alternative would have required excluding Supermarket or sourcing a different reference IDF — both worse outcomes. The repair is traceable to the v22.1 source.
- **Reversal cost:** re-run v03_simulate_refs.py after obtaining a clean IDFVersionUpdater output (requires investigating the IDFVersionUpdater bug for this specific coil type). Would require a new cluster job and re-extraction of reference_eui.parquet.

### OQ-R5-4 — Output:SQLite mode change (Simple → SimpleAndTabular) — 2026-06-11
- **Decision:** added `Output:SQLite, SimpleAndTabular;` to 16 DOE prototype IDFs that had no SQL output block, and changed `Simple` → `SimpleAndTabular` in MT5 and Supermarket IDFs. `TabularDataWithStrings` is required for EUI extraction from `AnnualBuildingUtilityPerformanceSummary`.
- **Why:** without SimpleAndTabular mode, eplusout.sql exists but has no `TabularDataWithStrings` table, making EUI extraction impossible. All 31 reference EUI values must be extracted the same way.
- **Reversal cost:** none — the modification is in `refs_work_v23/` (work copies, not the read-only originals in `docs/`).

### OQ-R5-5 — CP-V1 ruling: Phase A complete with 0/19 pass; storeys-heuristic root cause documented — 2026-06-11
- **Decision:** CP-V1 issued with full findings. The 0/19 PASS at ±5% round-trip gate is attributable to the `_floor_count_from_zones` heuristic in `v01_inventory_refs.py` overcounting zones as floors for large multi-zone DOE prototypes (e.g., Hospital: 55 zones parsed as 55 storeys vs actual 5 floors → footprint/floor = 19890/55 = 361 m² instead of 19890/5 = 3978 m²). This causes OpenUBEM's counterpart IDFs to be single-floor models with tiny footprints, producing correct energy intensity per-m²-modeled but very different total energy vs the full multi-story references. Report-only per V-R5-5; no model tuning.
- **Why:** per V-R5-5 and V-R5-8, the comparison gate is report-only and Phase A results must be reported honestly. The deviation pattern is systematic and explainable; it is not a random model failure.
- **Reversal cost:** to re-run with corrected story counts, the user would need to provide the actual DOE prototype story counts (or use the IDF geometry to compute correct floor counts), re-run V04, re-submit counterpart fleet, and regenerate the roundtrip report. Story count corrections are dataset metadata — no code logic change needed, just the `storeys` column in `ref_inventory.csv`.

### OQ-R5-6 — DQ-2 ruled overnight: storeys metadata fixed and V04+V05 re-run as V05b — 2026-06-11
- **Decision:** resolved DQ-2 myself under V-R5-8 instead of leaving it for the morning: dispatched a V05b executor to derive correct storey counts from the reference IDF geometry (z-coordinates, cross-checked against documented DOE prototype counts; STOP on conflict), regenerate the 23 counterpart IDFs, resimulate them on the cluster (`fleets/val2d/`), and regenerate the round-trip table in `results/`. Reference EUIs (`reference_eui.parquet`) are unaffected and reused.
- **Why:** without the fix, the Level-2 conclusion is "comparison invalid due to a metadata error" — which answers nothing the user asked. The standing zero-fail mandate (R4 entry C11 precedent) says diagnose → fix at generation → resimulate. The fix is dataset metadata only (storeys column), no model or gates code change, so V-R5-5 report-only is preserved. The 4 thermal-runaway counterparts (College, 3 data centers) are likely the same footprint bug (huge loads on a 10–55× too-small area) and must be re-diagnosed after the fix.
- **Reversal cost:** none — the original 0/19 table is preserved in §8 V05 and superseded, not deleted; the re-run costs one small cluster array (~23 jobs).

### OQ-R5-7 — Data-centre ITE counterparts: ground-temp patch attempted, 3/3 remain N/A — 2026-06-11
- **Decision:** (a) added `Site:GroundTemperature:BuildingSurface` (Buffalo monthly: −10,−8,−3,5,12,18,22,21,16,9,2,−6 °C) to LargeDataCenterHighITE, LargeDataCenterLowITE, SmallDataCenterHighITE counterpart IDFs before submission; (b) after job 955829 confirmed all 3 still failed, stripped 88 `SHADING:SITE:DETAILED` objects and re-submitted as job 955853 — still 3/3 FAILED; (c) ruled these 3 permanently N/A under the "physically impossible" exemption.
- **Root cause of N/A:** `CalcHeatBalanceInsideSurf` temperature divergence (10⁵–10⁶ °C) during EnergyPlus sizing warmup is caused by equipment loads of 1076–5382 W/m² in a single-zone IdealAir model. The EnergyPlus heat-balance solver diverges when extreme ITE loads are applied before the HVAC system operates during warmup — a known limitation of the IdealAir architecture at ITE load densities. SmallDataCenterLowITE (430 W/m²) succeeded, confirming the threshold. Capping equipment loads would yield a counterpart EUI <10% of reference, making the comparison meaningless.
- **Why N/A is warranted:** The "physically impossible" clause in the zero-fail mandate covers this: the OpenUBEM IdealAir + plain ElectricEquipment architecture cannot model ITE buildings with load densities >~800 W/m². The reference uses `ElectricEquipment:ITE:AirCooled` with dedicated CRAC/CRAH HVAC — a fundamentally different thermal accounting paradigm. Simulating these 3 types correctly would require adding ITE-specific HVAC to OpenUBEM (R6+ scope).
- **Reversal cost:** adding ITE-class HVAC to the OpenUBEM archetype library (significant R6 scope). The 3 N/A entries are documented in `roundtrip_report.md` with full failure diagnosis.

### OQ-R5-8 — DQ-3 ruled overnight: CP-V1b accepted as the Level-2 conclusion (option 1 + option 2 in V13) — 2026-06-11
- **Decision:** accepted the corrected round-trip table (1/23 PASS at ±5%, SmallHotel; 3 data-centre counterparts permanently N/A per OQ-R5-7) as the final Level-2 simulation deliverable for R5 (DQ-3 option 1). Additionally, DQ-3 option 2 — a fuel-basis-corrected supplementary column (counterpart cooling ÷ COP 3.5, heating × 1.19; report math only, no code change) — is folded into the V13 synthesis report so both raw and basis-corrected views appear side by side. Option 3 (proper HVAC parameterization) stays R6 scope. Also accepted the executor's FUEL_COLS fix (District Heating missing from the counterpart EUI extraction filter — script-local in `v05b_fix_storeys.py`, no `openubem/` code touched).
- **Why:** the comparison is now *valid* (storeys fixed: Hospital −93.7%→−12.2%, College runaway resolved), which was V05b's objective. The honest finding is "single-zone IdealAir archetypes deviate 12–309% from detailed DOE prototypes at individual-building level, with a known thermal-vs-electricity cooling basis asymmetry" — exactly what a report-only gate exists to surface (V-R5-5: never tune to pass). Option 2 costs minutes inside V13; options requiring model changes are forbidden tonight.
- **Reversal cost:** zero — all artifacts preserved; an R6 calibration plan can reuse `reference_eui.parquet`, the corrected `ref_inventory.csv`, and the val2d counterpart fleet as-is.

### OQ-R5-9 — V11 pilot closed with one V-R5-6 deviation and a funnel-count erratum — 2026-06-12
- **Decision:** V11 (NYC city-centre pilot, 738 buildings) is closed as complete: 738/738 simulated on cluster (jobs 956522/957268_1/957302), F12 gates ALL PASS, CBECS NE gates report-only (CV(RMSE) 89.5% / NMBE −34.9% / R² 0.78 / KS_D 0.287). Two items flagged for morning review rather than asked overnight: (a) **V-R5-6 deviation** — before the cluster repair job 957268_1 was submitted, a local diagnostic EnergyPlus run of the repaired way_265302069 IDF was performed; the cluster output is the authoritative artifact, but the local run did happen and is recorded in §8 V11 deviations. (b) **Funnel erratum** — V10's Overpass `out count` (619) undercounts because it misses OSM relations; actual fleet was 738. V12 cell counts in the approved matrix are therefore lower bounds, not exact — no re-approval requested since larger-than-expected cells only strengthen the cases.
- **Why:** both items are factual records, not pending decisions; V12 can proceed under the existing approvals.
- **Reversal cost:** none — artifacts preserved; if the user wants the local diagnostic run expunged from the narrative it stays in the plan log as history only.

### OQ-R5-10 — V12 executor decisions: fleet.lst format, streamed fetch, CBECS column alias — 2026-06-12
- **Decision:** three V12 pipeline decisions made without escalation because each is a mechanics fix, not a spec change: (a) **fleet.lst entries use the IDF filename stem** (underscore format `way_220649876`, Unix LF via `write_bytes`) — the sbatch script builds `idfs/<entry>.idf`, so slash-format osm_ids can never match; this is what V11 did and what job 957366's 1778/1778 `cp` failures proved. (b) **Cluster result fetch is a single streamed remote `find | tar --null -T -` pipe written to a local .tgz** — enumerating per-building paths in the ssh command dies on the Windows 32 767-char command-line limit at fleet sizes ≥ ~150 (WinError 206 / truncated-quote errors). (c) **CBECS gates are computed on `total_eui_kwh_m2` aliased to `eui_kwh_m2`** — `compute_validation_gates` accepts only `eui_kwh_m2`/`site_eui_kwh_m2` and Step 5 emits `total_eui_kwh_m2`; the alias is applied to a copy inside the validation scripts only, `openubem/` code untouched. All three are baked into `scripts/validation/v12_cell_pipeline.py` for nyc_suburban and nyc_rural.
- **Why:** (a) and (b) are bug fixes with a single correct answer; (c) total EUI (sum of end-use EUIs) is the site-EUI quantity the V11 CBECS comparison used, so the alias preserves comparability across cells.
- **Reversal cost:** none for (a)/(b); for (c), gates are report-only per V-R5-5 and can be recomputed from the shipped `05_results.*` with any other column choice.

### OQ-R5-11 — nyc_rural closed with F12 plausibility FAIL (QSR band artifact) and a 198<201 funnel under-count — 2026-06-12
- **Decision:** V12.nyc_rural is closed as complete despite the F12 EUI-plausibility gate reading FAIL (98.48%, 195/198). The 3 out-of-band buildings are all QuickServiceRestaurant at 1100.2-1101.2 kWh/m2/yr -- 10% above the generic [25,1000] upper bound, tightly clustered, with cooling EUI ~495-512 each (cooking/refrigeration-driven). This is an archetype-band artifact amplified by small n (3/198 = 1.5% > the 1% tolerance), not a malfunction: real-world QSR site EUIs routinely exceed 1000. No tuning, no exclusion, no band change performed -- the FAIL is reported as-is per the V-R5-5 never-tune principle. Separately, the live fetch returned 198 buildings vs the V10 probe of 201 -- the first downward deviation from a probe count (OQ-R5-9 assumed probes were strict lower bounds); attributed to OSM edits between probe and fetch dates. Neither item blocked the zero-fail mandate (198/198 simulated successfully, job 964352).
- **Why:** the binding overnight rules are zero-fail E+ completion (met) and no model tuning (honoured). Whether the plausibility band should be archetype-aware (e.g., a higher cap for food-service archetypes) is a spec question for the manager, not an executor call.
- **Reversal cost:** none -- if the manager widens the band or adds an archetype-aware cap, the gate recomputes from the shipped `05_results.csv` in seconds; the underlying results are untouched.
- **Manager ruling (2026-06-12, V-R5-8):** the generic [25,1000] band STAYS for all R5 cells -- widening it after seeing a FAIL is the tune-to-pass move V-R5-5 forbids. The FAIL stands as reported. V13 must carry one explanatory line (QSR cooking/refrigeration loads legitimately exceed the generic cap; 3/198 amplified by small n). An archetype-aware plausibility band is an R6 gate-spec proposal, noted alongside DQ-1 for the user.

## Questions deferred to the user (nothing blocks on these)

### DQ-2 — Fix storeys heuristic and re-run Level-2 round-trip? — 2026-06-11 — **RESOLVED overnight by OQ-R5-6 (fix + re-run dispatched)**
`_floor_count_from_zones` in `v01_inventory_refs.py` counts unique zone names as floors for DOE prototype IDFs, producing wildly incorrect story counts (Hospital=55, HotelSmall=67). The DOE prototype actual story counts are well-documented (Hospital=5, HotelSmall=4, HotelLarge=6, ApartmentHighRise=10, etc.). A corrected `ref_inventory.csv` with the actual story counts would allow a meaningful re-run of V04+V05. Cost: ~2–3 hours. Needed to draw any real conclusion from the Level-2 round-trip comparison.

### DQ-3 — V05b result: 1/23 PASS — accept as CP-V1b or investigate further? — 2026-06-11 — **RESOLVED overnight by OQ-R5-8 (option 1 accepted; option 2 folded into V13; option 3 = R6)**
V05b corrected the storeys bug and achieved 1/23 PASS (SmallHotel at -4.5%) vs 0/19 in V05. Key findings: (a) 3 data-centre N/A are architecturally permanent (OQ-R5-7); (b) counter-cooling is 3–8× reference cooling because IdealAir reports thermal cooling removed while references report electricity consumed by DX systems (same fuel-basis asymmetry as V06); (c) several over-predictions (SmallOffice +309%, MediumOffice +95%, College +56%) stem from the IdealAir over-cooling/over-heating simple parameterization vs the reference's detailed zone-by-zone schedule; (d) HighriseApartment -84% remains even with correct 10 storeys because the counterpart is a single-zone box vs the reference's 10-floor multi-unit building. Options: (1) accept 1/23 as CP-V1b conclusion — the comparison is IdealAir-vs-full-HVAC across all types, expected gap; (2) explore a fuel-basis-corrected gate (divide counterpart cooling by COP≈3.5, multiply counterpart heating by gas-factor ~1.19) to see how many pass on a corrected basis — no code change, just report math; (3) open R6 to add proper HVAC parameterization. None of these block Phase C.

### DQ-1 — Open an R6 deep-calibration plan? — 2026-06-11
Phase B's verdict: CBECS NE gates fail because the comparison is mismatched (Boston commercial-office fleet vs full NE stock). Closing the gap properly would mean an R6 plan: PBA2-only + Boston-climate CBECS sub-sample, vintage-matched internal-gain/setpoint schedules, and a COP/fuel-basis conversion layer at the *reporting* level (gates code still untouched). Significant scope — your call whether/when.
