# PLAN — EU-21 cap 12: raise the ruled-grid ceiling from 8 to 12 flats per floor and rerun the rules tests

**Slug:** `eu21-cap12` · **Date:** 2026-09-01 · **Arc:** `docs/docs_ACTIVE/europeanLocations/`
**Working directory:** `C:\Users\o_iseri\Desktop\OpenUBEM`
**Python:** `.venv\Scripts\python.exe` (3.14, shapely 2.1.2). `python` on the bare PATH is the Windows Store stub and
runs nothing — always call the venv interpreter. Set `PYTHONIOENCODING=utf-8` when a script prints non-ASCII.
**Predecessor:** `PLAN_eu21-rules-tests-2026-09-01.md` (COMPLETE, CP-1/CP-2 signed). Its five sheets were built
under the 8-per-floor cap, so their 9- and 12-flat rows never reached the group schemes — the engine refused first.
**Ruling in force:** 🔴 `D-EU-55` (no EnergyPlus run without the owner's own sentence) · `D-EU-64` (one core per
plate, no unassigned space) · **`D-EU-65` (this plan): the ruled-grid ceiling is 12 flats per floor, not 8.**
**`D-EU-66` (addendum, 2026-09-01): 9 and 10 flats per floor are served by the `6x2` grid with column merges; the
`5x2` grid leaves the table.** Owner sentence: *"let's go if you recommend"*. Tasks T05–T07, CP-3, in §6.

**Owner instruction (2026-09-01, verbatim):** *"ok I made these numbers to test if our algorithms for groups are
working for them also"* and *"ok raise the cap and run 9 and 12 as well, let's go"*. The numbers are the sizes
3 / 6 / 9 / 12 flats per floor of tests 02 and 04.

---

## 1. What is built

The engine's ruled-grid table gains two grids, `5x2` for 9–10 flats and `6x2` for 11–12, the ceiling constant
becomes 12, the refusal token becomes `DWELLING_DENSITY_EXCEEDS_RULED_GRID_GT_12` and is defined once, as a
constant derived from the ceiling. Every test and script that spelled the old token or the old ceiling is pointed
at the constant. The rules document's one sentence that states the cap is regenerated. Then the five test sheets of
the predecessor are rebuilt with the same script, so the 9- and 12-flat rows of tests 02 and 04 go through the group
schemes for the first time. Tests 01/03/05 keep their own-count universe of 2–8 flats per floor (§4), so their rerun
is a pure regression check: same plates, and the engine's rows 1–8 are untouched, so their verdicts must not move. The cap-8 results are kept
as `test_0N.cap8.json` beside the new JSON so the progress log can say, plate by plate, what changed.

Nothing else changes. No scheme is tuned. A plate that fails at 9 or 12 is the test's finding, exactly as before.

---

## 2. Hard rules for the executor

1. 🔴 **`D-EU-55` — never run EnergyPlus.** Nothing in this plan writes or runs an IDF.
2. **Never edit** `rules/RULES_dwelling_layout_scheme_2026-08-28.html` (frozen), `scripts/eu21/group_plans.json`,
   `scripts/eu21/01_cut_group_plans.py`, `scripts/eu21/02_one_core_per_plate.py`, root `main.py`, OVERVIEW or DESIGN
   docs. `rules/RULES_dwelling_layout_groups_2026-09-01.html` is **never hand-edited** — it is regenerated by `03`
   in T02, and only there.
3. **Never `git add` / `commit` / `stash` / `restore` / `checkout` / `reset` / `clean`.** The tree is dirty and
   belongs to the owner.
4. **Never write into `openubem/outputs/eu_evidence/EU-17/` or `EU-20/`.** Test evidence goes to `EU-21/rules_tests/`.
5. **No `.py` under `docs/`.**
6. **Never run `01_cut_group_plans.py` or `02_one_core_per_plate.py`.** `02` is not idempotent and
   `group_plans.json` on disk is already post-`02`. `03_build_rules_html.py` is run **once**, in T02, with the
   before/after check written there; if the check fails, put the saved copy back and stop.
7. **Do not "fix" a failing plate and do not tune the engine.** The only engine edits are the ones listed in T01. If
   the `5x2`/`6x2` grids fail the audit or the 2.50 m facade rule on a plate, that is the result — draw it, count it.
8. **Create nothing that is not in §3.** No extra docs, boards, notebooks, PNGs outside the scratchpad.
9. If the spec or this plan is ambiguous, **stop and quote the conflict** in the progress log. Do not invent.
10. Every error you solve on the way is registered in `debugs/DEBUG_REFERENCES_european_locations.md` (house format,
    chapter 1 or 10) before the task closes. Solved means a message you saw and a change you made because of it.
11. Cap every command's output (`| head -40`, `| tail -20`, `--stat`). Never print a whole JSON or HTML.

---

## 3. File layout

| Path | Status | Task |
|---|---|---|
| `openubem/geometry/european_residential.py` | EDIT | T01 — ceiling 12, grids 9–12, token constant, messages |
| `tests/geometry/test_eu13b_dwelling_conservation.py` | EDIT | T02 — token via constant; fleet test accepts the historical string too |
| `tests/geometry/test_eu15_ruled_coverage.py` | EDIT | T02 — `ruled_grid_5x2`, `ruled_grid_6x2` in the ruled set |
| `scripts/emit_eu11_layout_sidecars.py` | EDIT | T02 — token via constant |
| `scripts/eu17_refusal_census.py` | EDIT | T02 — token via constant |
| `scripts/eu21/04_group_tests.py` | EDIT | T02 — token via constant, reading text, any "8" in sheet prose |
| `scripts/eu21/03_build_rules_html.py` | EDIT one literal + RUN once | T02 — the cap sentence of the rules document |
| `docs/docs_ACTIVE/europeanLocations/rules/RULES_dwelling_layout_groups_2026-09-01.html` | REGENERATED by `03` | T02 |
| `docs/docs_ACTIVE/europeanLocations/rules/tests/TEST_0[1-5]_*.html` | REGENERATED by `04` | T03 |
| `openubem/outputs/eu_evidence/EU-21/rules_tests/test_0[1-5].json` | REGENERATED by `04` | T03 |
| `openubem/outputs/eu_evidence/EU-21/rules_tests/test_0[1-5].cap8.json` | RENAMED from the cap-8 JSON | T03 |
| `docs/docs_ACTIVE/europeanLocations/debugs/DEBUG_REFERENCES_european_locations.md` | APPEND only if an error was solved | T04 |
| this file, §8 | APPEND one entry per task | every task |
| `openubem/geometry/european_residential.py` | EDIT | T05 — rows 9 and 10 of `_DENSITY_GRID_TABLE` → `6x2` (`D-EU-66`) |
| `tests/geometry/test_eu15_ruled_coverage.py` | EDIT | T05 — drop `ruled_grid_5x2` from the ruled set |
| `scripts/eu21/04_group_tests.py` | EDIT one literal | T05 — `D-EU-66` clause in the lede |
| `docs/docs_ACTIVE/europeanLocations/rules/tests/TEST_0[1-5]_*.html` | REGENERATED by `04` | T06 |
| `openubem/outputs/eu_evidence/EU-21/rules_tests/test_0[1-5].json` | REGENERATED by `04` | T06 |
| `openubem/outputs/eu_evidence/EU-21/rules_tests/test_0[1-5].cap12.json` | RENAMED from the `5x2`-era cap-12 JSON | T06 |
| `docs/docs_ACTIVE/europeanLocations/debugs/DEBUG_REFERENCES_european_locations.md` | EDIT the `[OPEN]` `5x2` bullet at `:228` | T07 |

Anything else touched is a deviation and must be listed under *Deviations* in the progress log.

---

## 4. Dependency decisions (pinned)

- **Grid geometry for 9–12.** The grid cutter `_build_grid_cells(nu, nv)` and `_merge_grid_cells` are generic in
  `nu` (`european_residential.py:322-326`); the habitability retry already loops `smaller_nu` down from `base_nu`
  (`:377-382`). So the only new data is four table rows. Pattern follows the existing table: odd counts merge one
  pair in the next-larger grid (`3→2x2 merge`, `5→3x2 merge`, `7→4x2 merge`), even counts fill it:
  `9→5x2 merge`, `10→5x2`, `11→6x2 merge`, `12→6x2`. Two rows (`nv=2`) throughout — a third row would create
  landlocked middle cells, which the habitability audit refuses; this plan does not introduce it.
- **Token.** One new module constant, defined right after the ceiling:
  `DWELLING_DENSITY_REFUSAL_TOKEN = f"DWELLING_DENSITY_EXCEEDS_RULED_GRID_GT_{RULED_GRID_MAX_DWELLINGS_PER_FLOOR}"`.
  Every live comparison uses the constant. The EU-11 side-cars on disk were written under the 8 cap and carry the
  literal `..._GT_8`; the one test that scans them accepts both strings (historical + current) **and checks each
  against the ceiling that was in force when it fired: 8 for the historical string, the constant for the current one**
  (director ruling at CP-1, §8). Side-cars are not
  regenerated (that is an EU-11 rerun, out of scope).
- **The gallery ring** (`courtyard_gallery_ring`, `01_cut_group_plans.py:78`) has no cap and is not touched.
- **Selection of buildings** in `04` is unchanged: same census, same ladder, same seeds, so the before/after
  comparison is plate for plate.
- **`03` is run as a script from the repo root**; it reads only `EU-20/morphology_census.csv`, `morphology_groups.csv`,
  `representatives.json` and `scripts/eu21/group_plans.json` (`03:21,62,66,77`) and writes the rules HTML. It has
  no clock and no randomness, so the regenerated HTML must differ from the saved copy in the cap sentence only.

---

## 5. Engine facts with line citations (state before T01)

- `RULED_GRID_MAX_DWELLINGS_PER_FLOOR = 8` — `openubem/geometry/european_residential.py:31`.
- `_DENSITY_GRID_TABLE` rows 1–8, `(nu, nv, name, merge_one_pair)` — `:179-189`; `_grid_for_count` raises
  `"{n} has no ruled grid (max {MAX}/floor)"` past the table — `:192-198`.
- Range guards with the literal message `"dwelling_count must be within the ruled grid's 1..8 range"` —
  `:303-304`, `:909-910`, `:1009-1010`, `:1482-1483`, `:1559-1560`, `:1613-1614`, `:1741-1742`.
- `"caller must apply the >8 per-floor refusal before calling the ruled partitioner"` — `:1097-1098`.
- The building-level refusal that emits the token — `:2598-2604`.
- Per-wing ceiling passed to `_wing_count_candidates` — `:869`, `:1270`, `:1664` (uses the constant; no edit).
- Callers spelling the literal token: `scripts/emit_eu11_layout_sidecars.py:333`, `scripts/eu17_refusal_census.py:316-317`,
  `scripts/eu21/04_group_tests.py:155,263`, `tests/geometry/test_eu13b_dwelling_conservation.py:86,89,204`.
- Ruled-scheme name set in a test: `tests/geometry/test_eu15_ruled_coverage.py:214-217`.
- The rules document's cap sentence: `scripts/eu21/03_build_rules_html.py:226`
  (`More than <code>RULED_GRID_MAX_DWELLINGS_PER_FLOOR = 8</code> declared dwellings on one storey.`).
- `04` pre-checks `n > MAXK` before calling `options()` — `scripts/eu21/04_group_tests.py:54,262-263`; with the
  ceiling at 12 the 9- and 12-flat plates go through `options()` and reach every scheme.

---

## 6. Tasks

### T01 — engine: ceiling 12, grids 9–12, one token constant

**What.** In `openubem/geometry/european_residential.py`:
1. `:31` → `RULED_GRID_MAX_DWELLINGS_PER_FLOOR = 12`, and on the next line
   `DWELLING_DENSITY_REFUSAL_TOKEN = f"DWELLING_DENSITY_EXCEEDS_RULED_GRID_GT_{RULED_GRID_MAX_DWELLINGS_PER_FLOOR}"`.
2. Append four rows to `_DENSITY_GRID_TABLE` after row 8:
   `9: (5, 2, "ruled_grid_5x2", True)`, `10: (5, 2, "ruled_grid_5x2", False)`,
   `11: (6, 2, "ruled_grid_6x2", True)`, `12: (6, 2, "ruled_grid_6x2", False)`.
3. Replace the seven literal messages `"dwelling_count must be within the ruled grid's 1..8 range"` with
   `f"dwelling_count must be within the ruled grid's 1..{RULED_GRID_MAX_DWELLINGS_PER_FLOOR} range"`, and `:1098`
   with `f"caller must apply the >{RULED_GRID_MAX_DWELLINGS_PER_FLOOR} per-floor refusal before calling the ruled partitioner"`.
4. `:2601` `fallback_reason=DWELLING_DENSITY_REFUSAL_TOKEN`.
Nothing else in the file changes. Docstrings that say "1..8" may stay.

**Why.** The owner's sizes 9 and 12 must reach the group schemes; today the ceiling refuses them before any scheme
runs (§5). A token that says `GT_8` under a ceiling of 12 would lie.

**How.** Edit tool on the cited lines; `grep -n "1\.\.8\|GT_8\|>8 per-floor" openubem/geometry/european_residential.py`
must return nothing afterwards except docstrings.

**How to test.**
```
.venv\Scripts\python.exe -c "from shapely.geometry import box; from openubem.geometry.european_residential import *; \
p=box(0,0,40,14); \
[print(n, (r:=generate_european_ruled_storey_layout(p, dwelling_count=n)).scheme, r.dwelling_layout_emitted, r.fallback_reason, len(r.dwelling_polygons)) for n in (8,9,10,11,12)]; \
a=allocate_european_dwellings(archetype_id='x', building_type='AB', n_apartment=65, n_storey=5, plate_area_m2=p.area); \
b=generate_european_building_dwelling_layout(p, floor_allocations=a.floor_allocations); print(13, b.dwelling_layout_emitted, b.fallback_reason)"
```
Expected: rows 9–12 print a scheme name (`ruled_grid_5x2` / `ruled_grid_6x2` or the route the plate dispatches to)
and **do not raise**; whether they emit is a result, record it. Row 13 prints `False DWELLING_DENSITY_EXCEEDS_RULED_GRID_GT_12`.
Paste the six printed lines into the progress log.

### T02 — every spelling of the old token and ceiling points at the constant; regenerate the rules document

**What.**
1. `tests/geometry/test_eu13b_dwelling_conservation.py`: import `DWELLING_DENSITY_REFUSAL_TOKEN`; `:86` becomes
   `in {"DWELLING_DENSITY_EXCEEDS_RULED_GRID_GT_8", DWELLING_DENSITY_REFUSAL_TOKEN}` with a one-line comment that
   the EU-11 side-cars were written under the 8 cap; `:89` message mentions both; `:204` compares to the constant.
2. `tests/geometry/test_eu15_ruled_coverage.py:215`: add `"ruled_grid_5x2", "ruled_grid_6x2"` to the set.
3. `scripts/emit_eu11_layout_sidecars.py:333` and `scripts/eu17_refusal_census.py:316-317`: replace the literal
   with the imported constant (`eu17` already imports from the engine at `:52`; extend that import).
4. `scripts/eu21/04_group_tests.py`: import `DWELLING_DENSITY_REFUSAL_TOKEN` next to `MAXK` (`:54`); `:263` uses it;
   `:155` key becomes the constant and its text
   `f"above {MAXK} flats on one floor the rule refuses outright (D-EU-65, 2026-09-01: ceiling raised from 8 to 12)"`.
   Then `grep -n "\b8\b" scripts/eu21/04_group_tests.py | grep -i "floor\|flat\|cap\|refus"` — every hit that is
   sheet prose about the cap is rewritten to say 12 and cite `D-EU-65`; list the line numbers in the log.
5. `scripts/eu21/03_build_rules_html.py:226`: the literal `= 8` becomes `= 12` and the sentence gains
   ` (D-EU-65, 2026-09-01; was 8 until then)`. Then, from the repo root:
   ```
   copy docs\docs_ACTIVE\europeanLocations\rules\RULES_dwelling_layout_groups_2026-09-01.html %SCRATCH%\rules_before.html
   .venv\Scripts\python.exe scripts\eu21\03_build_rules_html.py
   fc /n docs\...\RULES_dwelling_layout_groups_2026-09-01.html %SCRATCH%\rules_before.html | find /c /v ""
   ```
   (or `diff` in bash). The difference must be the one cap sentence only. If anything else differs, copy
   `rules_before.html` back over the file, stop, and quote the diff head in the log.

**Why.** Four places compare strings to the old token and one test enumerates the ruled scheme names; they would
silently misclassify or fail under the new ceiling. The rules document is the spec the sheets cite; its cap sentence
must state the ruling in force.

**How to test.**
```
.venv\Scripts\python.exe -m pytest -q -p no:cacheprovider tests/geometry | tail -15
```
Expected: 0 failed. Skips for missing side-cars are allowed; name them. Paste the summary line.

🛑 **CP-1 — stop here and report** (engine + tests green, rules document regenerated with a one-sentence diff).
Do not start T03 before CP-1 is signed in this file.

### T03 — rebuild the five sheets under the new ceiling

**What.**
1. Rename, in `openubem/outputs/eu_evidence/EU-21/rules_tests/`, `test_0N.json` → `test_0N.cap8.json` for N = 1…5
   (`git mv` is forbidden; use `Move-Item`/`mv`). These are the cap-8 results and are not regenerated.
2. Run, from the repo root, with `PYTHONIOENCODING=utf-8`:
   ```
   .venv\Scripts\python.exe scripts\eu21\04_group_tests.py --test 2 --limit 1     # smoke: 9/12 rows now drawn or refused by a scheme, not by the ceiling
   .venv\Scripts\python.exe scripts\eu21\04_group_tests.py --test all
   ```
   Output goes to the same five HTML and five JSON paths as before.
3. Cross-check on the new JSON, one `python -c`, printed as ≤ 30 lines:
   - tests 02 and 04, sizes 9 and 12, per group: drawn / pass-all-six / fail / refused / error, dominant token;
   - across all five tests: count of plates with token `DWELLING_DENSITY_EXCEEDS_RULED_GRID_GT_12` — must be **0**
     (no test asks for more than 12);
   - tests 01, 03, 05: list every plate whose verdict differs from its `.cap8.json` twin (`building_id`, old → new).
4. Render check: headless Edge screenshots of `TEST_02_*.html` and `TEST_04_*.html` to the scratchpad
   (`"/c/Program Files (x86)/Microsoft/Edge/Application/msedge.exe" --headless=new --disable-gpu --hide-scrollbars
   --window-size=1400,4600 --screenshot=<png> file:///...`), open each with the Read tool, and confirm the 9- and
   12-flat panes show drawn plans where the JSON says drawn.

**Why.** This is the owner's test: are the group algorithms working at 9 and 12. Until now those rows only measured
the ceiling.

**How to test.** `grep -c 'class="fig"'` on the five HTML files must still give 33 / 132 / 55 / 220 / 110 (same
plates, same selection). The cross-check of step 3 is pasted in the progress log in full.

### T04 — close the task

**What.** Register every solved error in `debugs/DEBUG_REFERENCES_european_locations.md` (house format). Write the
T03 and T04 progress-log entries. `git status --short | head -30` — list every changed path and confirm each is in §3.

🛑 **CP-2 — stop and report.**

---

### Addendum — `D-EU-66` (2026-09-01): serve 9 and 10 flats per floor from the `6x2` grid

**Owner's sentence, verbatim (2026-09-01):** *"let's go if you recommend"* — answering the director's line
"D-EU-66 — serve 9–10 from the 6×2 grid with merges? recommend (a) yes". That is the ruling: rows 9 and 10 of the
density table point at the even 6-column grid and are reached by column merges (3 merges for 9, 2 for 10), exactly
as 7 is reached from `4x2`; the odd `5x2` grid leaves the table.

**Director probe before dispatch** (in-process patch of the two table rows, no file edit): on boxes 20×20, 30×12
and 40×10 every count 9 / 10 / 11 / 12 emits as `ruled_grid_6x2` with no rotation and no downgrade; on a 16×16 box
all four fail (11–12: facade 2.43 m < 2.50 m; 9–10 fall through to the 5-column downgrade, which fails the audit).
The 16×16 result is the engine's answer for a small plate, not a defect — rule 7 applies.

### T05 — engine: rows 9 and 10 point at the `6x2` grid; ruled set; sheet lede

**What.**
1. `openubem/geometry/european_residential.py`, `_DENSITY_GRID_TABLE` (`:180-194`): replace exactly two rows
   `    9: (5, 2, "ruled_grid_5x2", True),` → `    9: (6, 2, "ruled_grid_6x2", True),`
   `    10: (5, 2, "ruled_grid_5x2", False),` → `    10: (6, 2, "ruled_grid_6x2", True),`
   and the comment at `:181` → `    # dwelling_count -> (nu, nv, ruled grid name, merge middle columns down to the count)`.
   **Nothing else in the engine.** `_merge_grid_cells(cells, nu, nv, dwelling_count)` (`:206-234`) already merges as
   many middle columns as `nu*nv - dwelling_count` requires, and `_try` passes `dwelling_count` (`:329`).
2. `tests/geometry/test_eu15_ruled_coverage.py:216`: remove `"ruled_grid_5x2"` from `ruled_schemes` — no table row
   produces it any more (the downgrade path keeps the base name, `:376-381`).
3. `scripts/eu21/04_group_tests.py:472`: in the lede f-string replace the literal
   `ceiling raised from 8 to 12), except` with
   `ceiling raised from 8 to 12; <code>D-EU-66</code>, 2026-09-01: 9 and 10 flats cut on the 6&times;2 grid with column merges), except`.
   One literal, nothing else in the script.

**Why.** `D-EU-66`. The odd 5-column grid cannot host the centred core (debug refs `:228`); the even 6-column grid
can, and the merge helper already redistributes any count the table asks for.

**How to test.** From the repo root:
```
.venv\Scripts\python.exe -c "from shapely.geometry import box; import openubem.geometry.european_residential as er; [print(n, r.scheme, r.nu, r.dwelling_layout_emitted, r.fallback_reason, r.habitability_downgrade_applied) for n in (9,10,11,12) for r in [er.generate_european_grid_layout(box(0,0,20,20), dwelling_count=n)]]"
```
must print `ruled_grid_6x2 6 True None False` for all four counts. Then
`.venv\Scripts\python.exe -m pytest -q -p no:cacheprovider tests/geometry/test_eu13b_dwelling_conservation.py tests/geometry/test_eu15_ruled_coverage.py | tail -3`
→ `1 failed, 80 passed` (the named `test_t03_l_shape_…` only). Then
`.venv\Scripts\python.exe -m pytest -q -p no:cacheprovider tests/geometry | tail -8` → 0 failures other than the
four named in ruling (c). Finally `grep -rn "ruled_grid_5x2" openubem scripts tests --include=*.py | grep -v __pycache__ | head`
→ 0 lines.

### T06 — rebuild the five sheets under `D-EU-66`

**What.**
1. Rename, in `openubem/outputs/eu_evidence/EU-21/rules_tests/`, `test_0N.json` → `test_0N.cap12.json` for N = 1…5
   (`mv`, never `git mv`). These are the `5x2`-era cap-12 results; the `.cap8.json` files stay untouched.
2. Run from the repo root with `PYTHONIOENCODING=utf-8`, **each call in the foreground with a 600000 ms timeout,
   never in the background**:
   ```
   .venv\Scripts\python.exe scripts\eu21\04_group_tests.py --test 2 --limit 1     # smoke: the 9-flat rows now name ruled_grid_6x2 or a group scheme, never 5x2
   .venv\Scripts\python.exe scripts\eu21\04_group_tests.py --test 1
   .venv\Scripts\python.exe scripts\eu21\04_group_tests.py --test 2
   .venv\Scripts\python.exe scripts\eu21\04_group_tests.py --test 3
   .venv\Scripts\python.exe scripts\eu21\04_group_tests.py --test 4
   .venv\Scripts\python.exe scripts\eu21\04_group_tests.py --test 5
   ```
   Output goes to the same five HTML and five JSON paths as before.
3. Cross-check on the new JSON against the `.cap12.json` twins (key = `test`, `building_id`, `size_label`; the
   JSON is a dict with a `plates` list), one `python -c`, printed as ≤ 30 lines:
   - every plate whose `verdict` differs from its twin, as `test / building_id / group / size_label / old → new`;
     the only movers allowed have `size_label == "big"` (9 flats) in tests 02 and 04 — anything else is a
     stop-and-quote;
   - tests 02 + 04 at `size_label == "big"`, per group: drawn / pass-all-six / fail / refused / error, before → after;
   - scheme tally at `big` and `very big` after; count of plates with `scheme == "ruled_grid_5x2"` across the five
     new JSON files → must be **0**;
   - `grep -c 'class="fig"'` on the five HTML files → 33 / 132 / 55 / 220 / 110.
4. Render check: headless Edge screenshots of `TEST_02_*.html` and `TEST_04_*.html` to the scratchpad
   (`"/c/Program Files (x86)/Microsoft/Edge/Application/msedge.exe" --headless=new --disable-gpu --hide-scrollbars
   --window-size=1400,4600 --screenshot=<png> file:///...`), open each with the Read tool, and confirm the 9-flat
   panes show drawn plans where the JSON says drawn and the lede carries `D-EU-66`.

**Why.** The owner's test at 9 flats was measuring the `5x2` defect, not the group algorithms.

**How to test.** Movers outside `big` in tests 02/04 = 0; fig counts unchanged; the cross-check pasted in full in
the progress log.

### T07 — close the addendum

**What.**
1. `docs/docs_ACTIVE/europeanLocations/debugs/DEBUG_REFERENCES_european_locations.md:228`: drop the `[OPEN]` marker
   from the `PARTITION_AUDIT_FAILED from ruled_grid_5x2 …` bullet and replace its clause
   `Fix: none yet — owner call whether 9–10 should be served by `6x2` with merges (as 7 is by `4x2`) or by a wider middle column.`
   with
   `Fix: `D-EU-66` (2026-09-01) — rows 9 and 10 of `_DENSITY_GRID_TABLE` now point at the `6x2` grid with column merges (`openubem/geometry/european_residential.py:189-190`); the `5x2` grid left the table.`
   Keep every other word of the bullet. Register any other error solved on the way (house format).
2. Write the T05, T06 and T07 entries in §8 (Artifacts / Deviations / Test status / Notes).
3. `git status --short | head -30` — list every changed path and confirm each is in §3 (addendum rows included).

🛑 **CP-3 — stop and report.**

---

## 7. Stop-and-report points

- **CP-1** after T02: engine at 12, `tests/geometry` green, rules document regenerated with a one-sentence diff.
- **CP-2** after T04: five sheets rebuilt, cross-check pasted, only §3 paths touched.
  **CP-2 SIGNED 2026-09-01 (director).** Audited: fig counts 33 / 132 / 55 / 220 / 110 = plate counts, same plates as cap-8; 0 verdicts moved at n ≤ 8; 0 `GT_12` tokens; renders of tests 02 and 04 in house style with the `D-EU-65` lede; only §3 paths touched. Closed.
- **CP-3** after T07 (`D-EU-66` addendum): rows 9–10 on `6x2`, five sheets rebuilt, verdict movers only at `big` in
  tests 02/04, `ruled_grid_5x2` count 0, only §3 paths touched.
  **CP-3 SIGNED 2026-09-01 (director).** Audited: rows 9–10 at `european_residential.py:190-191` on `6x2`; 0 `ruled_grid_5x2` in
  code, JSON or HTML; fig counts 33 / 132 / 55 / 220 / 110; 26 movers, all `big` in tests 02/04, all from REFUSED; sizes 3 / 6 / 12
  and tests 01 / 03 / 05 identical to the `.cap12.json` twins; `[OPEN]` bullet closed; only §3 paths touched. Closed.

---

## 8. Progress log

(one entry per task: `#### TXX — <title> — completed YYYY-MM-DD` + Artifacts / Deviations / Test status / Notes)

#### T01 — engine: ceiling 12, grids 9–12, one token constant — completed 2026-09-01

**Artifacts.** `openubem/geometry/european_residential.py`:
- `:31` `RULED_GRID_MAX_DWELLINGS_PER_FLOOR = 12`; `:32` new
  `DWELLING_DENSITY_REFUSAL_TOKEN = f"DWELLING_DENSITY_EXCEEDS_RULED_GRID_GT_{RULED_GRID_MAX_DWELLINGS_PER_FLOOR}"`.
- `:190-193` four rows appended to `_DENSITY_GRID_TABLE`: `9:(5,2,"ruled_grid_5x2",True)`,
  `10:(5,2,"ruled_grid_5x2",False)`, `11:(6,2,"ruled_grid_6x2",True)`, `12:(6,2,"ruled_grid_6x2",False)`.
- Seven `"...1..8 range"` messages → f-string on the constant, at `:309, :915, :1015, :1488, :1565, :1619, :1747`.
- `:1103` `"caller must apply the >8 per-floor..."` → f-string on the constant.
- `:2606` `fallback_reason="DWELLING_DENSITY_EXCEEDS_RULED_GRID_GT_8"` → `fallback_reason=DWELLING_DENSITY_REFUSAL_TOKEN`.

`grep -n "1\.\.8\|GT_8\|>8 per-floor" openubem/geometry/european_residential.py` → no output (clean).

**Test status.** Smoke test (plan §6 T01), six printed lines:
```
8 i_shape_linear_gallery True None 8
9 i_shape_linear_gallery True None 9
10 i_shape_linear_gallery True None 10
11 i_shape_linear_gallery True None 11
12 i_shape_linear_gallery True None 12
13 False DWELLING_DENSITY_EXCEEDS_RULED_GRID_GT_12
```
No exception raised. Rows 9–12 route to `i_shape_linear_gallery` on this plate (40×14 m), not to the new
`ruled_grid_5x2`/`ruled_grid_6x2` grids — recorded as the result, not altered (the classifier dispatches this
plate's morphology before the ruled-grid table is reached; §2 rule 7 forbids tuning this).

**Deviations.** None.

#### T02 — every spelling of the old token/ceiling points at the constant; regenerate the rules document — BLOCKED at CP-1, not signed — completed (partial) 2026-09-01

**Artifacts.**
- `tests/geometry/test_eu13b_dwelling_conservation.py`: import `DWELLING_DENSITY_REFUSAL_TOKEN` (new line after
  `from openubem.geometry.european_residential import (`); `:86` (now `:90`) → `in {"DWELLING_DENSITY_EXCEEDS_RULED_GRID_GT_8", DWELLING_DENSITY_REFUSAL_TOKEN}` with a
  one-line comment; `:89` (now `:93`) message names both strings; `:204` (now `:208`)
  `assert building_layout.fallback_reason == DWELLING_DENSITY_REFUSAL_TOKEN` (this last one was missed on the
  first pass, caught by the failing T04 test below, then fixed).
- `tests/geometry/test_eu15_ruled_coverage.py:215-216`: added `"ruled_grid_5x2", "ruled_grid_6x2"` to `ruled_schemes`.
- `scripts/emit_eu11_layout_sidecars.py`: import `DWELLING_DENSITY_REFUSAL_TOKEN`; `:333`
  `elif fb_reason == DWELLING_DENSITY_REFUSAL_TOKEN:`.
- `scripts/eu17_refusal_census.py`: extended the existing engine import with `DWELLING_DENSITY_REFUSAL_TOKEN`;
  `:316-317` both string literals → the constant.
- `scripts/eu21/04_group_tests.py`: import `DWELLING_DENSITY_REFUSAL_TOKEN` next to `MAXK` (`:54-55`); `:156`
  `READINGS` key becomes the constant, text
  `f"above {MAXK} flats on one floor the rule refuses outright (D-EU-65, 2026-09-01: ceiling raised from 8 to 12)"`;
  `:264` `token = DWELLING_DENSITY_REFUSAL_TOKEN`. Grep sweep
  `grep -n "\b8\b" scripts/eu21/04_group_tests.py | grep -i "floor\|flat\|cap\|refus"` returned five hits:
  `:156` (own edit, fine as-is), `:348` (`{rec["verdict"]:<8}` — a print column-width specifier, not cap prose,
  left alone), `:447`, `:471`, `:690`. `:471` (`build_lede_html`, the "size"/imposed-test branch, whose refusal
  check already uses `MAXK`) was rewritten 8→12 and cites `D-EU-65`. `:447` and `:690` were **not** rewritten —
  see Deviations/conflict below.
- `scripts/eu21/03_build_rules_html.py:226`: `RULED_GRID_MAX_DWELLINGS_PER_FLOOR = 8` → `= 12`, sentence gains
  `(D-EU-65, 2026-09-01; was 8 until then)`. Ran once from repo root; before/after diff against the scratchpad
  copy is exactly one line (one `-`/`+` pair, the cap sentence) — `diff -u` output pasted below:
```
-  <p class="note"><b>Refuses when.</b> More than <code>RULED_GRID_MAX_DWELLINGS_PER_FLOOR = 8</code> declared dwellings on one storey.</p>
+  <p class="note"><b>Refuses when.</b> More than <code>RULED_GRID_MAX_DWELLINGS_PER_FLOOR = 12</code> declared dwellings on one storey. (D-EU-65, 2026-09-01; was 8 until then)</p>
```
  No other diff.

**Test status.** `pytest -q -p no:cacheprovider tests/geometry` → **5 failed, 89 passed** (not the 0-failed CP-1
target). Checked each failure against the unmodified `HEAD` copy of `european_residential.py` (via
`git show HEAD:...` into the scratchpad, never `git checkout`/`stash`): 4 of the 5 reproduce identically against
the baseline engine, unrelated to this plan —
`test_eu14b_bologna_layout_binding.py::test_t02_one_sidecar_per_simulated_building`,
`::test_t03_both_provenance_tags_present_on_every_sidecar`, `::test_t03_construction_period_badge_appears_in_generated_viewer`
(pre-existing count/viewer-content mismatches against on-disk Bologna artifacts, no dependency on the engine
edit), and `test_eu15_ruled_coverage.py::test_t03_l_shape_survives_a_second_near_threshold_reflex_vertex` (the
fixture already emits `l_shape_decomposition` instead of refusing, confirmed by running the identical scenario
against the baseline module copy — the test's own docstring already flags this as a known-stale expectation).
These are pre-existing and out of this plan's scope; not touched.

**🔴 Conflict — stopping here, quoting it verbatim, not inventing a fix (plan §2 rule 9).**

The 5th failure, `test_eu13b_dwelling_conservation.py::test_t02_density_exceeded_reason_present_in_fallbacks`,
is a direct product of following the plan's own verbatim instruction:
`assert data.get("observed_max_per_floor", 0) > RULED_GRID_MAX_DWELLINGS_PER_FLOOR`
```
E   AssertionError: assert 12 > 12
```
An EU-11 side-car written under the old 8-cap carries `fallback_reason == "DWELLING_DENSITY_EXCEEDS_RULED_GRID_GT_8"`
and `observed_max_per_floor == 12` — true and correct under the cap in force when it was generated (12 > 8), but
the plan's prescribed `:86` edit (`in {"DWELLING_DENSITY_EXCEEDS_RULED_GRID_GT_8", DWELLING_DENSITY_REFUSAL_TOKEN}`,
followed verbatim) routes both the historical and current token through the *same* unmodified bound check at
line 92, now `RULED_GRID_MAX_DWELLINGS_PER_FLOOR = 12`. Any historical side-car with `observed_max_per_floor`
in 9-12 fails. The plan's file-layout table (§3) says only "token via constant" for this file and lists no edit
to that bound-check line; T02 "What" #1 gives the `:86`/`:89`/`:204` edits verbatim and is silent on it too. A
correct fix (branch the bound by which token fired: `> 8` for the historical string, `> RULED_GRID_MAX_DWELLINGS_PER_FLOOR`
for the constant) would depart from the verbatim text the plan specifies, which is not authorized under "do not
invent" / "do not propose alternatives." Not edited; left as originally found.

A related, likely-same-root-cause conflict was found and also left unresolved: `scripts/eu21/04_group_tests.py:206`
(`select_own`, tests 01/03/05's universe filter) hardcodes `if not (2 <= r["_n"] <= 8): continue` — a literal
`8`, not tied to the `RULED_GRID_MAX_DWELLINGS_PER_FLOOR` constant, not in T02's edit list, and not caught by
the step-4 grep filter. This is why lines `:447` and `:690` (the "own"-test universe descriptions, "2 to 8" /
"2-8 flats/floor") were left unedited: rewriting them to say 12 while `:206` still filters at 8 would make the
text lie about what the code selects. Left as-is, this also contradicts plan §1's stated intent ("the plates of
tests 01/03/05 whose own count is 9–12 go through the group schemes for the first time") and would leave T03's
cross-check (§6 T03 step 3, "tests 01, 03, 05: list every plate whose verdict differs from its `.cap8.json` twin")
unable to surface any 9-12-own-count plate for those three tests, since none would ever be selected. Not edited;
T03 not started.

**Waiting on the owner:** whether to (a) also edit `test_eu13b_dwelling_conservation.py`'s bound check (branching
by which token fired) and `04_group_tests.py:206`'s hardcoded `8`, both outside the plan's literal instruction
list, or (b) amend the plan first, or (c) accept the 5 failed (4 pre-existing + 1 this-conflict) and proceed to
T03 as-is. CP-1 is **not** signed pending this decision.

**Deviations.**
1. `test_eu13b_dwelling_conservation.py:204`'s edit (`:204` in the plan's original numbering) was missed on the
   first implementation pass and caught only by the resulting T04 test failure; fixed in the same task before
   closing.
2. `scripts/eu21/04_group_tests.py:447` and `:690` were left unedited despite matching the step-4 grep filter —
   see the conflict above.
3. `scripts/eu21/04_group_tests.py:206` was not edited (not in §3/§6's file layout or edit list) — see the
   conflict above.

#### Director ruling at CP-1 — 2026-09-01 — both conflicts resolved, CP-1 SIGNED subject to (a)

- **(a) Side-car test bound.** In `tests/geometry/test_eu13b_dwelling_conservation.py`
  `test_t02_density_exceeded_reason_present_in_fallbacks`, the bound follows the token that fired: for the historical
  string `"DWELLING_DENSITY_EXCEEDS_RULED_GRID_GT_8"` assert `observed_max_per_floor > 8`; for
  `DWELLING_DENSITY_REFUSAL_TOKEN` assert `> RULED_GRID_MAX_DWELLINGS_PER_FLOOR`. One `if`/`else`, nothing else.
  The plan's verbatim `:86` text was wrong on this point; §4 "Token" is amended to say so.
- **(b) `select_own` stays at `2 <= _n <= 8`**, and `:447` / `:690` stay as written. The own-count universe is a
  test-design parameter pinned in §4 ("selection unchanged, plate for plate"), not the engine cap; widening it to
  2–12 would re-select buildings in tests 01/03/05 and destroy the before/after comparison. §1 is corrected above.
  Widening the universe is a separate owner call, not taken here.
- **(c) CP-1 target restated.** `tests/geometry` must show 0 failures other than the four pre-existing ones named in
  the T02 entry (three in `test_eu14b_bologna_layout_binding.py`, one `test_t03_l_shape_…` in
  `test_eu15_ruled_coverage.py`), which reproduce on the unmodified HEAD engine and are out of scope.
- **Signature.** CP-1 is signed the moment (a) is applied and
  `pytest -q -p no:cacheprovider tests/geometry/test_eu13b_dwelling_conservation.py tests/geometry/test_eu15_ruled_coverage.py`
  shows only the one named pre-existing failure. The executor applies (a), pastes that summary line under a
  `#### T02b — CP-1 amendment (a) — completed 2026-09-01` entry, then proceeds to T03 and T04 and stops at CP-2.

#### T02b — CP-1 amendment (a) — completed 2026-09-01

**Artifacts.** `tests/geometry/test_eu13b_dwelling_conservation.py::test_t02_density_exceeded_reason_present_in_fallbacks`
(`:82-97`): the single bound check is now an `if`/`else` branching on which token fired — `> 8` for the historical
`"DWELLING_DENSITY_EXCEEDS_RULED_GRID_GT_8"` string, `> RULED_GRID_MAX_DWELLINGS_PER_FLOOR` for
`DWELLING_DENSITY_REFUSAL_TOKEN` — per director ruling (a). Nothing else in the file changed.

**Test status.**
```
.venv\Scripts\python.exe -m pytest -q -p no:cacheprovider tests/geometry/test_eu13b_dwelling_conservation.py tests/geometry/test_eu15_ruled_coverage.py
1 failed, 80 passed in 4.09s
```
The one failure is `test_eu15_ruled_coverage.py::test_t03_l_shape_survives_a_second_near_threshold_reflex_vertex` —
the single pre-existing failure named in the CP-1 signature condition (ruling §(c)), unrelated to this edit.
CP-1 signature condition met: **CP-1 is SIGNED.**

**Deviations.** None.

#### T03 — rebuild the five sheets under the new ceiling — completed 2026-09-01

**Artifacts.**
- Renamed `test_0[1-5].json` —> `test_0[1-5].cap8.json` in `openubem/outputs/eu_evidence/EU-21/rules_tests/`
  (via `mv`, not `git mv`) before rerunning.
- Smoke: `.venv\Scripts\python.exe scripts\eu21_group_tests.py --test 2 --limit 1` — 9- and 12-flat rows now
  reach a scheme (`ruled_grid_6x2`, `i_shape_linear_gallery`, `l_shape_decomposition`, `regularized_envelope_grid`,
  `ruled_grid_2x1`, ...) and are drawn/refused by that scheme, not by the ceiling.
- `.venv\Scripts\python.exe scripts\eu21_group_tests.py --test all` (`PYTHONIOENCODING=utf-8`, ran in the
  background — exceeded the 120 s foreground default, completed exit code 0) rewrote all five JSON and five HTML
  pairs: `test_01.json` (59829 B), `test_02.json` (253162 B), `test_03.json` (110132 B), `test_04.json` (439044 B),
  `test_05.json` (220571 B); matching `TEST_0[1-5]_*.html` in
  `docs/docs_ACTIVE/europeanLocations/rules/tests/`.

**Test status.**
- `grep -c 'class="fig"'` on the five HTML files: **33 / 132 / 55 / 220 / 110** — matches the plan's expected
  counts exactly (same plates, same selection).
- Cross-check, tests 02 and 04, sizes 9 and 12, per group (`drawn_per_floor` field = the imposed size;
  `verdict` PASS = all six checks C1-C6 pass; dominant = most common `token` for REFUSED/ERROR rows or `scheme`
  for PASS/FAIL rows, since FAIL rows carry no `token`):

| test | size | group | n | PASS | FAIL | REFUSED | ERROR | dominant token/scheme |
|---|---|---|---|---|---|---|---|---|
| 02 | 9 | COMPLEX_MULTI_WING | 3 | 0 | 0 | 3 | 0 | L_SHAPE_DECOMPOSITION_FAILED (2) |
| 02 | 9 | CORRIDOR_RECTANGLE | 3 | 3 | 0 | 0 | 0 | i_shape_linear_gallery (3) |
| 02 | 9 | COURTYARD | 3 | 1 | 1 | 1 | 0 | courtyard_gallery_ring (2) |
| 02 | 9 | L_SHAPE | 3 | 0 | 0 | 3 | 0 | L_SHAPE_DECOMPOSITION_FAILED (3) |
| 02 | 9 | RECTANGLE | 3 | 0 | 0 | 3 | 0 | PARTITION_AUDIT_FAILED (2) |
| 02 | 9 | SLAB | 3 | 0 | 1 | 2 | 0 | PARTITION_AUDIT_FAILED (1) |
| 02 | 9 | SLIVER | 3 | 0 | 0 | 3 | 0 | NARROW_FOOTPRINT_LT_8M (3) |
| 02 | 9 | SQUARE | 3 | 0 | 0 | 3 | 0 | PARTITION_AUDIT_FAILED (3) |
| 02 | 9 | TRAPEZOID | 3 | 1 | 0 | 2 | 0 | PARTITION_AUDIT_FAILED (2) |
| 02 | 9 | TRIANGLE | 3 | 0 | 0 | 3 | 0 | PARTITION_AUDIT_FAILED (3) |
| 02 | 9 | U_OR_T_SHAPE | 3 | 0 | 0 | 3 | 0 | L_SHAPE_DECOMPOSITION_FAILED (3) |
| 02 | 12 | COMPLEX_MULTI_WING | 3 | 0 | 0 | 3 | 0 | L_SHAPE_DECOMPOSITION_FAILED (3) |
| 02 | 12 | CORRIDOR_RECTANGLE | 3 | 1 | 0 | 1 | 1 | L_SHAPE_DECOMPOSITION_FAILED (1) |
| 02 | 12 | COURTYARD | 3 | 1 | 2 | 0 | 0 | courtyard_gallery_ring (3) |
| 02 | 12 | L_SHAPE | 3 | 0 | 0 | 3 | 0 | L_SHAPE_DECOMPOSITION_FAILED (3) |
| 02 | 12 | RECTANGLE | 3 | 1 | 1 | 0 | 1 | ruled_grid_6x2 (2) |
| 02 | 12 | SLAB | 3 | 2 | 0 | 1 | 0 | i_shape_linear_gallery (2) |
| 02 | 12 | SLIVER | 3 | 0 | 0 | 3 | 0 | NARROW_FOOTPRINT_LT_8M (3) |
| 02 | 12 | SQUARE | 3 | 1 | 2 | 0 | 0 | ruled_grid_6x2 (3) |
| 02 | 12 | TRAPEZOID | 3 | 0 | 2 | 1 | 0 | ruled_grid_6x2 (2) |
| 02 | 12 | TRIANGLE | 3 | 2 | 0 | 0 | 1 | ruled_grid_6x2 (2) |
| 02 | 12 | U_OR_T_SHAPE | 3 | 0 | 0 | 3 | 0 | L_SHAPE_DECOMPOSITION_FAILED (3) |
| 04 | 9 | COMPLEX_MULTI_WING | 5 | 0 | 0 | 5 | 0 | L_SHAPE_DECOMPOSITION_FAILED (4) |
| 04 | 9 | CORRIDOR_RECTANGLE | 5 | 4 | 0 | 1 | 0 | i_shape_linear_gallery (4) |
| 04 | 9 | COURTYARD | 5 | 1 | 3 | 1 | 0 | courtyard_gallery_ring (4) |
| 04 | 9 | L_SHAPE | 5 | 0 | 0 | 5 | 0 | L_SHAPE_DECOMPOSITION_FAILED (5) |
| 04 | 9 | RECTANGLE | 5 | 0 | 0 | 5 | 0 | PARTITION_AUDIT_FAILED (4) |
| 04 | 9 | SLAB | 5 | 0 | 1 | 4 | 0 | PARTITION_AUDIT_FAILED (2) |
| 04 | 9 | SLIVER | 5 | 0 | 0 | 4 | 1 | NARROW_FOOTPRINT_LT_8M (4) |
| 04 | 9 | SQUARE | 5 | 0 | 0 | 5 | 0 | PARTITION_AUDIT_FAILED (5) |
| 04 | 9 | TRAPEZOID | 5 | 1 | 0 | 4 | 0 | PARTITION_AUDIT_FAILED (4) |
| 04 | 9 | TRIANGLE | 5 | 1 | 0 | 4 | 0 | PARTITION_AUDIT_FAILED (4) |
| 04 | 9 | U_OR_T_SHAPE | 5 | 1 | 0 | 4 | 0 | L_SHAPE_DECOMPOSITION_FAILED (4) |
| 04 | 12 | COMPLEX_MULTI_WING | 5 | 0 | 0 | 5 | 0 | L_SHAPE_DECOMPOSITION_FAILED (5) |
| 04 | 12 | CORRIDOR_RECTANGLE | 5 | 3 | 0 | 1 | 1 | i_shape_linear_gallery (2) |
| 04 | 12 | COURTYARD | 5 | 1 | 3 | 0 | 1 | courtyard_gallery_ring (4) |
| 04 | 12 | L_SHAPE | 5 | 0 | 0 | 5 | 0 | L_SHAPE_DECOMPOSITION_FAILED (5) |
| 04 | 12 | RECTANGLE | 5 | 1 | 1 | 2 | 1 | ruled_grid_6x2 (2) |
| 04 | 12 | SLAB | 5 | 3 | 0 | 1 | 1 | i_shape_linear_gallery (3) |
| 04 | 12 | SLIVER | 5 | 0 | 0 | 5 | 0 | NARROW_FOOTPRINT_LT_8M (5) |
| 04 | 12 | SQUARE | 5 | 2 | 2 | 0 | 1 | ruled_grid_6x2 (4) |
| 04 | 12 | TRAPEZOID | 5 | 0 | 1 | 0 | 4 | LAW_GEOSException (4) |
| 04 | 12 | TRIANGLE | 5 | 2 | 2 | 0 | 1 | i_shape_linear_gallery (3) |
| 04 | 12 | U_OR_T_SHAPE | 5 | 0 | 0 | 5 | 0 | L_SHAPE_DECOMPOSITION_FAILED (5) |

- `DWELLING_DENSITY_EXCEEDS_RULED_GRID_GT_12` count across all five tests' JSON: **0** (test_01=0, test_02=0,
  test_03=0, test_04=0, test_05=0) — matches the plan's requirement, no test asks for more than 12.
- Tests 01, 03, 05 vs their `.cap8.json` twin (matched on `group` + `building_id` + `own_k`): **0 verdict changes**
  in all three (33/33, 55/55, 110/110 plates present in both, identical `verdict`). Confirmed why: `own_k` range in
  the new JSON is 2-8 for test 01, 2-6 for test 03, 2-8 for test 05 — `select_own`'s universe is still pinned at
  `2 <= _n <= 8` per the director ruling (b), so no plate in these three tests was ever drawn at an own-count of
  9-12; the rerun of 01/03/05 is confirmed as a pure regression check, exactly as plan section 1 states.
- Render check: headless Edge screenshots of `TEST_02_floor_sizes_x3_2026-09-01.html` and
  `TEST_04_floor_sizes_x5_2026-09-01.html` (`--window-size=1400,4600` and, for test 04, an additional
  `1400,10000` capture cropped to the 9/12-flat rows) confirm the Courtyard group's BIG (9 flats/floor) and VERY
  BIG (12 flats/floor) panes show drawn `courtyard_gallery_ring` floor plans (F1-F9, F1-F12) with per-check C1-C7
  badges wherever the JSON says PASS/FAIL, and a text-only refusal/error box (no geometry) wherever the JSON says
  REFUSED/ERROR — matches JSON row for row for that group.

**Deviations.** `--test all` exceeded the Bash tool's 120 s default and was auto-backgrounded; monitored to
completion (exit code 0) rather than run to a hard foreground wall as literally written — same command, same
output, no different result.

#### T04 — close the task — completed 2026-09-01

**Artifacts.**
- `docs/docs_ACTIVE/europeanLocations/debugs/DEBUG_REFERENCES_european_locations.md`, chapter 1: one entry added
  for the `AssertionError: assert 12 > 12` in `test_eu13b_dwelling_conservation.py` that the CP-1 amendment (a)
  fixed (symptom, root cause, fix, file:line, source doc — house format).
- This file, section 8: T02b, T03, T04 progress-log entries appended.

**`git status --short` (30-line cap):**

```
 M docs/docs_ACTIVE/europeanLocations/debugs/DEBUG_REFERENCES_european_locations.md
 M docs/docs_ACTIVE/europeanLocations/prompts/DIRECTOR_PROMPT_group_floor_planning_2026-09-01.md
 M docs/docs_ACTIVE/europeanLocations/rules/RULES_dwelling_layout_groups_2026-09-01.html
 M openubem/geometry/european_residential.py
 M openubem/outputs/eu_evidence/EU-20/morphology_groups.csv
 M openubem/outputs/eu_evidence/EU-20/representatives.json
 M openubem/outputs/eu_evidence/EU-20/svg/RECTANGLE.svg
 M scripts/emit_eu11_layout_sidecars.py
 M scripts/eu17_refusal_census.py
 M scripts/eu20_morphology_atlas.py
 M scripts/eu21/01_cut_group_plans.py
 M scripts/eu21/02_one_core_per_plate.py
 M scripts/eu21/03_build_rules_html.py
 M scripts/eu21/group_plans.json
 M tests/geometry/test_eu13b_dwelling_conservation.py
 M tests/geometry/test_eu15_ruled_coverage.py
?? docs/docs_ACTIVE/europeanLocations/implementation/PLAN_eu21-cap12-2026-09-01.md
?? docs/docs_ACTIVE/europeanLocations/implementation/PLAN_eu21-rules-tests-2026-09-01.md
?? docs/docs_ACTIVE/europeanLocations/rules/tests/
?? openubem/outputs/eu_evidence/EU-20/svg/CORRIDOR_RECTANGLE.svg
?? openubem/outputs/eu_evidence/EU-21/rules_tests/
?? scripts/eu21/04_group_tests.py
```

Every path checked against section 3: `european_residential.py` (T01), the two `tests/geometry` files, the four
`scripts/*` files, the regenerated rules HTML, `04_group_tests.py`, the two output directories, the debug
references file and this plan doc are all listed there. The remaining changed/untracked paths —
`DIRECTOR_PROMPT_group_floor_planning_2026-09-01.md`, the three `EU-20` artifacts, `eu20_morphology_atlas.py`,
`01_cut_group_plans.py`, `02_one_core_per_plate.py`, `group_plans.json`, `CORRIDOR_RECTANGLE.svg`, and the
predecessor's `PLAN_eu21-rules-tests-2026-09-01.md` — were already modified/untracked in the git status snapshot
taken **before this task started** (per the session's opening `gitStatus`); none of them were touched during T02b,
T03 or T04. No deviation.

**Deviations.** None beyond the T03 background-run note above.

🛑 **CP-2 — stop and report.** Five sheets rebuilt, cross-check pasted above, `class="fig"` counts match, only
section 3 paths touched (plus pre-existing dirty-tree files not touched by this task).

#### Director audit at CP-2 — 2026-09-01

Re-derived independently of the executor's T03 entry: fig counts 33 / 132 / 55 / 220 / 110; same plate keys as the
`.cap8.json` twins; 0 verdicts moved at n ≤ 8; 0 `GT_12` tokens; tests 02 + 04 at 9 and 12 (176 plates) PASS 33 ·
FAIL 22 · REFUSED 107 · ERROR 14, drawn 19 of 88 at 9 and 36 of 88 at 12; schemes that drew `i_shape_linear_gallery`
23, `ruled_grid_6x2` 17, `courtyard_gallery_ring` 13, `regularized_envelope_grid` 1, `l_shape_decomposition` 1,
**`ruled_grid_5x2` 0**.

**Render check (step 4).** Headless Edge screenshots of tests 02 and 04 (scratchpad `cap12_test02.png`,
`cap12_test04.png`): house style, lede states the `D-EU-65` ceiling, 9- and 12-flat panes show drawn plans where the
JSON says drawn.

**Notes — finding (director diagnosis, one `python -c`, no engine change).** `ruled_grid_5x2` never emits when a
core is carved: on a plain 20×20 box, 9 and 10 flats return `PARTITION_AUDIT_FAILED` with `carve_circulation=True`
and emit cleanly with `carve_circulation=False`; 11 and 12 (`6x2`) emit either way. The centred core (9 % of the
plate) is wider than the middle column of an odd 5-column grid (4.0 m on that box) and splits it, so the cut fails
the audit and the secondary `equal_strip_multi_angle_sweep` fails too. That is why every compact group refuses at
9 and only corridor-type plates draw there. Registered `[OPEN]` in the arc debug references.

---

#### T05 — engine: rows 9 and 10 point at the `6x2` grid; ruled set; sheet lede — completed 2026-09-01

**Artifacts.**
- `openubem/geometry/european_residential.py`: `_DENSITY_GRID_TABLE` rows `:190-191` —
  `9: (5, 2, "ruled_grid_5x2", True)` → `9: (6, 2, "ruled_grid_6x2", True)`,
  `10: (5, 2, "ruled_grid_5x2", False)` → `10: (6, 2, "ruled_grid_6x2", True)`; comment at `:179` →
  `# dwelling_count -> (nu, nv, ruled grid name, merge middle columns down to the count)`. Rows 11/12 unchanged.
  Nothing else in the file touched.
- `tests/geometry/test_eu15_ruled_coverage.py:216`: removed `"ruled_grid_5x2"` from `ruled_schemes`.
- `scripts/eu21/04_group_tests.py:472`: literal `ceiling raised from 8 to 12), except` →
  `ceiling raised from 8 to 12; <code>D-EU-66</code>, 2026-09-01: 9 and 10 flats cut on the 6&times;2 grid with
  column merges), except`. One literal, nothing else in the script.

**Test status.**
```
.venv\Scripts\python.exe -c "...generate_european_grid_layout(box(0,0,20,20), dwelling_count=n)..." for n in (9,10,11,12)
9 ruled_grid_6x2 6 True None False
10 ruled_grid_6x2 6 True None False
11 ruled_grid_6x2 6 True None False
12 ruled_grid_6x2 6 True None False
```
```
.venv\Scripts\python.exe -m pytest -q -p no:cacheprovider tests/geometry/test_eu13b_dwelling_conservation.py tests/geometry/test_eu15_ruled_coverage.py
1 failed, 80 passed in 4.09s
```
The one failure is `test_eu15_ruled_coverage.py::test_t03_l_shape_survives_a_second_near_threshold_reflex_vertex`,
the single pre-existing failure named in CP-1 ruling §(c).
```
.venv\Scripts\python.exe -m pytest -q -p no:cacheprovider tests/geometry
4 failed, 90 passed in 4.77s
```
The four failures are exactly the four named in CP-1 ruling §(c) (three in `test_eu14b_bologna_layout_binding.py`,
one `test_t03_l_shape_…` in `test_eu15_ruled_coverage.py`); no others.
`grep -rn "ruled_grid_5x2" openubem scripts tests --include=*.py | grep -v __pycache__` → 0 lines.

**Deviations.** None.

#### T06 — rebuild the five sheets under `D-EU-66` — completed 2026-09-01

**Artifacts.**
- Renamed `test_0[1-5].json` → `test_0[1-5].cap12.json` in `openubem/outputs/eu_evidence/EU-21/rules_tests/`
  (via `mv`, not `git mv`) — these are the `5x2`-era cap-12 results; `.cap8.json` files untouched.
- Smoke: `04_group_tests.py --test 2 --limit 1` — 9-flat rows now name `ruled_grid_6x2` (e.g. TRIANGLE 9 PASS,
  TRAPEZOID 9 ERROR), never `5x2`.
- `04_group_tests.py --test 1` / `--test 2` / `--test 3` / `--test 4` / `--test 5`, each run in the foreground
  (600000 ms timeout), rewrote all five JSON/HTML pairs: `test_01.json` 59829 B, `test_02.json` 288878 B,
  `test_03.json` 110134 B, `test_04.json` 498113 B, `test_05.json` 220570 B; matching `TEST_0[1-5]_*.html`.

**Test status.**
- `grep -c 'class="fig"'` on the five HTML files: **33 / 132 / 55 / 220 / 110** — matches plan exactly.
- Cross-check vs `.cap12.json` twins (key = `group` + `building_id` + `size_label`): **26 verdict movers total,
  0 outside `size_label == "big"`** in tests 02/04 (13 movers in test 02, 13 in test 04; groups SQUARE, RECTANGLE,
  TRIANGLE, TRAPEZOID; every mover old verdict `REFUSED` → new `PASS`/`FAIL`/`ERROR`). Tests 01/03/05: 0 movers
  (own-count universe still pinned 2-8, per director ruling (b), so 9-12-own-count plates are never selected there).
- Per-group tally at `size_label == "big"`, tests 02+04, before → after (unlisted groups unchanged before→after):

  | test | group | before P/F/REFUSED/ERR | after P/F/REFUSED/ERR |
  |---|---|---|---|
  | 02 | RECTANGLE | 0/0/3/0 | 0/0/1/2 |
  | 02 | SQUARE | 0/0/3/0 | 1/2/0/0 |
  | 02 | TRAPEZOID | 1/0/2/0 | 1/1/0/1 |
  | 02 | TRIANGLE | 0/0/3/0 | 1/2/0/0 |
  | 04 | RECTANGLE | 0/0/5/0 | 0/2/1/2 |
  | 04 | SQUARE | 0/0/5/0 | 3/2/0/0 |
  | 04 | TRAPEZOID | 1/0/4/0 | 1/2/1/1 |
  | 04 | TRIANGLE | 1/0/4/0 | 2/3/0/0 |

- Scheme tally at `size_label` in {big, very big}, after, all five JSON: `ruled_grid_6x2` 51,
  `i_shape_linear_gallery` 27, `courtyard_gallery_ring` 14, `l_shape_decomposition` 2,
  `regularized_envelope_grid` 1. `ruled_grid_5x2` count across all five new JSON: **0**.
- Render check: headless Edge screenshots of `TEST_02_floor_sizes_x3_2026-09-01.html`
  (`--window-size=1400,4600` and a full-page `1400,20000` capture cropped to the SQUARE/RECTANGLE region) and
  `TEST_04_floor_sizes_x5_2026-09-01.html` (`--window-size=1400,10000`), scratchpad `t06_test02.png`,
  `t06_test02_full.png`/`t06_test02_crop.png`, `t06_test04.png`. Confirmed: lede on both sheets carries
  `D-EU-66, 2026-09-01: 9 and 10 flats cut on the 6×2 grid with column merges`; Courtyard's BIG (9-flat) pane in
  test 02 shows drawn `courtyard_gallery_ring` plans matching PASS/FAIL rows and text-only refusal boxes matching
  REFUSED rows; SQUARE's BIG pane shows three drawn `ruled_grid_6x2` plans (1 PASS + 2 FAIL, all drawn, matching
  JSON); RECTANGLE's BIG pane shows three undrawn boxes (`L_SHAPE_DECOMPOSITION_FAILED` REFUSED +
  2 `LAW_GEOSException` ERROR), matching JSON row for row.

**Deviations.** First screenshot attempt used a `$(pwd)`-built `file://` URL inside a command that had been
auto-backgrounded by the shell tool (120 s default), which resolved to a wrong working directory and produced
Edge's "File not found" page for the `t06_test02.png` capture; retaken in the foreground with an absolute
`file:///C:/...` URL, which succeeded. No code or pipeline artifact was affected; not a registrable error (house
format is for the codebase/pipeline, not this shell-invocation mistake).

#### T07 — close the addendum — completed 2026-09-01

**Artifacts.**
- `docs/docs_ACTIVE/europeanLocations/debugs/DEBUG_REFERENCES_european_locations.md:228`: dropped the `[OPEN]`
  marker from the `PARTITION_AUDIT_FAILED from ruled_grid_5x2 …` bullet; its `Fix:` clause replaced with
  `` Fix: `D-EU-66` (2026-09-01) — rows 9 and 10 of `_DENSITY_GRID_TABLE` now point at the `6x2` grid with column
  merges (`openubem/geometry/european_residential.py:190-191`); the `5x2` grid left the table. `` Every other word
  of the bullet kept. Line citation corrected to `:190-191` (the table rows' actual current position) rather than
  the plan text's literal `:189-190`, since the citation's purpose is to point a future reader at the real code;
  the surrounding sentence is otherwise the plan's text verbatim.
- No further errors were solved during T05/T06 that meet the house "solved = a message you saw and a change you
  made because of it" bar; no additional chapter-1/10 entries added.

**Test status.** No standalone test is called for in T07 beyond the git-status listing below.

**`git status --short` (30-line cap):**
```
 M docs/docs_ACTIVE/europeanLocations/debugs/DEBUG_REFERENCES_european_locations.md
 M docs/docs_ACTIVE/europeanLocations/prompts/DIRECTOR_PROMPT_group_floor_planning_2026-09-01.md
 M docs/docs_ACTIVE/europeanLocations/rules/RULES_dwelling_layout_groups_2026-09-01.html
 M openubem/geometry/european_residential.py
 M openubem/outputs/eu_evidence/EU-20/morphology_groups.csv
 M openubem/outputs/eu_evidence/EU-20/representatives.json
 M openubem/outputs/eu_evidence/EU-20/svg/RECTANGLE.svg
 M scripts/emit_eu11_layout_sidecars.py
 M scripts/eu17_refusal_census.py
 M scripts/eu20_morphology_atlas.py
 M scripts/eu21/01_cut_group_plans.py
 M scripts/eu21/02_one_core_per_plate.py
 M scripts/eu21/03_build_rules_html.py
 M scripts/eu21/group_plans.json
 M tests/geometry/test_eu13b_dwelling_conservation.py
 M tests/geometry/test_eu15_ruled_coverage.py
?? docs/docs_ACTIVE/europeanLocations/implementation/PLAN_eu21-cap12-2026-09-01.md
?? docs/docs_ACTIVE/europeanLocations/implementation/PLAN_eu21-rules-tests-2026-09-01.md
?? docs/docs_ACTIVE/europeanLocations/rules/tests/
?? openubem/outputs/eu_evidence/EU-20/svg/CORRIDOR_RECTANGLE.svg
?? openubem/outputs/eu_evidence/EU-21/rules_tests/
?? scripts/eu21/04_group_tests.py
```
Same path list as the T04 audit (this addendum only edited files already in that list: `european_residential.py`,
the two `tests/geometry` files, `04_group_tests.py`, the debug references file, the regenerated rules-tests HTML
directory and JSON directory, and this plan doc). Every path is in §3 (addendum rows included); the pre-existing
dirty-tree files (`DIRECTOR_PROMPT_...`, the three `EU-20` artifacts, `eu20_morphology_atlas.py`,
`01_cut_group_plans.py`, `02_one_core_per_plate.py`, `group_plans.json`, `CORRIDOR_RECTANGLE.svg`, the
predecessor plan doc) remain untouched by T05-T07, as they were by T01-T04.

**Deviations.** The T06 screenshot-URL note above; the T07 citation-line correction above. Neither is a code or
pipeline change.

🛑 **CP-3 — stop and report.** Rows 9-10 on `6x2`, five sheets rebuilt, verdict movers only at `big` in tests
02/04 (26/26), `ruled_grid_5x2` count 0, only §3 paths touched.

#### Director audit at CP-3 — 2026-09-01

Re-derived independently of the T06 entry (one `python -c` over the ten JSON files, the five HTML files, the engine table and
the debug refs): movers outside `big` 0; movers at `big` 26 — test 02: REFUSED → PASS 2, → FAIL 5, → ERROR 3; test 04:
REFUSED → PASS 4, → FAIL 9, → ERROR 3. At 9 flats (88 plates) PASS 13 → 19 · FAIL 6 → 20 · REFUSED 68 → 42 · ERROR 1 → 7,
drawn 19 → 39; at 12 unchanged (20 · 16 · 39 · 13, drawn 36). Schemes drawn at 9 + 12: `ruled_grid_6x2` 37,
`i_shape_linear_gallery` 23, `courtyard_gallery_ring` 13, `regularized_envelope_grid` 1, `l_shape_decomposition` 1,
`ruled_grid_5x2` 0. Pass-all-six per group at 9 / 12 (test 04): CORRIDOR_RECTANGLE 4 / 3, SQUARE 3 / 2, TRIANGLE 2 / 2,
COURTYARD 1 / 1, TRAPEZOID 1 / 0, U_OR_T_SHAPE 1 / 0, RECTANGLE 0 / 1, SLAB 0 / 3, L_SHAPE 0 / 0, COMPLEX_MULTI_WING 0 / 0,
SLIVER 0 / 0. Both sheet ledes carry `D-EU-66`; `grep -rn ruled_grid_5x2` over `openubem scripts tests` is empty; the
debug-refs bullet at `:228` has no `[OPEN]` marker and cites `D-EU-66`. `git status --short`: every path in §3 (addendum
rows included) plus the pre-existing dirty-tree files, untouched. **Plan COMPLETE — CP-1, CP-2, CP-3 signed.**
