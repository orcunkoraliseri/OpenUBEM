# PLAN — EU-21 `T07`: three cutter defects, re-measure, and the RULES document update

**Slug:** `eu21-t07` · **Opened:** 2026-09-02 · **Director:** this session · **Executors:** fresh Sonnet, one per slice.
**Governing prompt:** `docs/docs_ACTIVE/europeanLocations/prompts/DIRECTOR_PROMPT_group_floor_planning_2026-09-01.md` (§2 the law, §3 how to run, §4 non-negotiables, §5 `T07` candidates).
**Predecessor:** `PLAN_eu21-direct-cutters-2026-09-01.md` (COMPLETE, CP-1…CP-5 signed).

**Owner's authorisation, 2026-09-02:** *"lets go by recommendation"* — in answer to the director's line
*"ratify DD-1…DD-8, order the `03` run, take T07 (a)(b)(c) — recommend all three."* Followed by
*"continuer jusqu'a la fin, vas-y"* — run the plan to the end without stopping for approval between tasks; the
checkpoints below are still reported, but the director does not wait for the owner at them.

**What that authorises, and nothing more:**
1. `DD-1`…`DD-8` of the predecessor plan §4 are **ratified**. They stop being director decisions and are in force for the tests.
2. One `03` run — the RULES document text is brought level with the code.
3. `T07` (a) wing junctions · (b) light-well / high-reflex refusals · (c) `C4` lobes. **Not** (d), **not** (e) beyond T04 §3.

**Rulings in force:** `D-EU-64` (one core, flats + core = plate, simple outlines) · `D-EU-65` (ceiling 12 flats/floor) ·
`D-EU-66` (9–10 on `6x2`, engine only) · `D-EU-67` (courtyard cores joined into one zone) · `D-EU-68` (flats are basic
thermal zones) · ratified `DD-1`…`DD-8` · 🔴 `D-EU-55` (no EnergyPlus without the owner's own sentence — nothing here runs one).

---

## 1. Hard rules for the executor

- Edit **only** the files named in the task you are executing. No others, for any reason.
- **Never** `git add` / `commit` / `stash` / `restore` / `checkout` / `reset` / `clean`. The tree is dirty and is the owner's.
- **Never** write into `openubem/outputs/eu_evidence/EU-17/` or `EU-20/`. New evidence goes to `EU-21/`.
- **Never** edit `rules/RULES_dwelling_layout_scheme_2026-08-28.html` (frozen; read its CSS only).
- **No generated HTML is ever hand-edited** — `rules/RULES_dwelling_layout_groups_2026-09-01.html` only through `03`,
  `rules/tests/TEST_0N_*.html` only through `04`.
- No `.py` under `docs/`. Create nothing that is not asked for — no extra docs, boards or reports.
- Every solved error is registered in `docs/docs_ACTIVE/europeanLocations/debugs/DEBUG_REFERENCES_european_locations.md`
  ch. 1, house format, **before the task closes**.
- Append one progress-log entry per task under §8 of this file: `#### TXX — <title> — completed YYYY-MM-DD` with
  Artifacts / Deviations / Test status / Notes.
- Do not propose alternatives — execute. If the plan is ambiguous, STOP and quote the conflict.
- A refusal that is **honest** (the plate genuinely cannot hold the zone) stays a refusal. Do not tune a constant to make
  a number look better; report it.
- Run everything from the repo root with `.venv\Scripts\python.exe` and `PYTHONIOENCODING=utf-8`.

---

## 2. File layout

| File | Role in this plan |
|---|---|
| `scripts/eu21/05_group_cutters.py` | 1427 lines. **The only file T01–T03 may edit.** |
| `scripts/eu21/04_group_tests.py` | Test driver. T04 runs it; edits only the contact-sheet title string (T04 step 3). |
| `scripts/eu21/03_build_rules_html.py` | `SPEC` dict + HTML writer. **The only file T05 may edit.** |
| `docs/.../rules/RULES_dwelling_layout_groups_2026-09-01.html` | Regenerated by `03` in T05. Never hand-edited. |
| `docs/.../rules/tests/TEST_0N_*.html` | Regenerated by `04` in T04. Never hand-edited. |
| `openubem/outputs/eu_evidence/EU-21/rules_tests/test_0N.json` | Overwritten by `04`; the current set is copied aside first (T04). |
| `openubem/outputs/eu_evidence/EU-21/cutter_demo/` | Demo + contact PNGs, scratch evidence. |
| `docs/.../debugs/DEBUG_REFERENCES_european_locations.md` | Every solved error, ch. 1. |

---

## 3. Measured baseline — re-derived by the director 2026-09-02 from the five `test_0N.json`, not copied

550 plates: **432 PASS · 50 FAIL · 68 REFUSED · 0 errors.**

Per group (PASS / FAIL / REFUSED): `SQUARE` `RECTANGLE` `TRIANGLE` `TRAPEZOID` 50/0/0 · `CORRIDOR_RECTANGLE` 46/0/4 ·
`SLAB` 43/4/3 · `SLIVER` 38/0/12 · `L_SHAPE` 35/6/9 · `U_OR_T_SHAPE` 32/10/8 · `COMPLEX_MULTI_WING` 23/21/6 ·
`COURTYARD` 15/9/26.

Failing checks over the 50 FAIL plates (a plate can break more than one):

| Check | n | by group |
|---|---|---|
| `C4` one room per flat / no overlap | **22** | COMPLEX 9 · U/T 7 · COURTYARD 2 · SLAB 2 · L 2 |
| `C6` 2.50 m facade | **18** | COMPLEX 11 · L 4 · U/T 3 |
| `C5` simple outline / point count | **17** | COMPLEX 11 · SLAB 2 · U/T 2 · COURTYARD 2 |
| `C2` exactly one circulation zone | **5** | COURTYARD 5 |

Refusal tokens over the 68 REFUSED plates:

| Token | n | by group |
|---|---|---|
| `BAND_LT_3M` | **30** | SLIVER 12 · COURTYARD 8 · CORRIDOR_RECTANGLE 4 · L 3 · U/T 3 |
| `WING_TREE_FAILED` | **29** | COURTYARD 12 · COMPLEX 6 · L 6 · U/T 3 · SLAB 2 |
| `FLAT_ENCLOSES_ZONE` | **6** | COURTYARD 5 · SLAB 1 |
| `CELL_EMPTY` | **3** | U/T 2 · COURTYARD 1 |

`BAND_LT_3M` is `T07` (d) — **honest, ordered to be left alone.** The three tasks below target `WING_TREE_FAILED`
(T01), `C5`/`C6` on wings (T02) and `C4` (T03). The five `COURTYARD` `C2` failures and the six `FLAT_ENCLOSES_ZONE`
refusals are **not** ordered: if a task happens to clear them, report it; do not chase them.

---

## 4. Code facts, with line citations — `scripts/eu21/05_group_cutters.py`

- `_split_wings()` `:887` — recursive `_split_once()` loop. **`:894`: `if len(finished) + len(pieces) > 8: raise Refusal("WING_TREE_FAILED")`** — the 8-piece cap. `:903` raises the same token when a piece cannot be split.
- `cut_wings()` `:1093` — `:1097` refuses when `len(wings) < 2`; `:1249` refuses when the assembled circulation is not a single hole-free Polygon.
- Circulation assembly `:1216`: `combined = _polygons_only(set_precision(unary_union([core_world] + band_geoms)...))`. Each band in `band_geoms` is an axis-aligned `box` built in **its own wing's frame** (`:1173-1181`) and only then rotated to world — so at an angled junction two bands cross at that angle and their union outline steps. This is `T07` (a).
- `:1163` `band_lo, u_far = minx - 0.5, minx + LANDING` (one-flat leaf arm, `DD-8` stub).
- `:1168` `band_lo = (min(reach) - LANDING) if reach else (minx - 0.5)` (root wing).
- **`:1170-1174`**: `if odd_through and xs and reach and xs[-1] >= max(reach) - 1e-9: u_far = xs[-1]` **else** `u_far = (max(reach) + LANDING) if reach else maxx`. The `else` branch pushes the band `LANDING` (1.50 m) **past the last cut, into the last cell**, so that flat is cut around the band's end and comes back as two lobes joined by a neck → `C4`. This is `T07` (c). The same overshoot exists at the low end via `:1168`.
- `_junction(w, parent_w)` `:979` — returns the junction point of a wing and its parent. `_connector()` `:626`, `_bridge()` `:645` — the fallbacks; `_bridge` is what draws the stepped joins that survive to the sheet.
- `exterior_group(poly)` `:1264` and `_dispatch_group(poly, k, grp2)` `:1293` — how a light-well plate (`DD-7`) is re-routed to its exterior's group. `cut()` `:1301` is the entry point.
- `cut_convex()` `:296`; the same `LANDING` overshoot pattern at `:364-365` — **out of scope here**, T03 does not touch `cut_convex`.
- Constants `:28-48`: `CORRIDOR_W = 1.80` · `LANDING = 1.50` · `BAND_MIN_W = MIN_FLAT_W = 3.0` · `COURTYARD_MIN_VOID_SIDE = 6.0` · `MIN_WING_W = 3.0` · `CONVEX_GROUPS` · `WING_GROUPS`.

`scripts/eu21/03_build_rules_html.py`: `ABSORB` `:149`, `CLOSE` `:165` (shared, the law — steps 5 and 6), `SPEC` `:174`
(per group `match` / `plate` / `core` / `cut` / `scheme` / `refuse`, each a list of one-line bullets). Sheet numbers are
`SPEC[g]["num"]`. **Step 3 of a sheet is the `core` list; step 4 is the `cut` list.**

---

## 5. Director decision taken with this plan (flagged for the owner, not yet ratified)

- **`DD-9`** — the RULES document's **drawings** are regenerated from the direct cutter, not only its text. Reason: the
  owner ordered the text to follow the code; leaving the eleven figures as engine + `apply_law()` output would put a
  cutter description beside a post-pass picture on the same sheet. T05 writes a **separate** `group_plans_cutter.json`
  and leaves `scripts/eu21/group_plans.json` untouched, so `01`/`02` are unaffected and the change is reversible by one
  constant. If the owner refuses `DD-9`, T05 is re-run with `USE_CUTTER = False` and only the text changes.

---

## 6. Tasks

### T01 — `T07` (b): stop refusing complex exteriors at eight pieces

**What.** `WING_TREE_FAILED` costs 29 of the 68 refusals, 12 of them `COURTYARD` light-well plates that `DD-7` rerouted
to their exterior's group. Raise the ceiling and give the decomposition a fallback instead of a refusal.

**Why.** Prompt §5 `T07` (b): *"light-well plates whose exterior has > 8 reflex vertices refuse `WING_TREE_FAILED`
(12 of the 26 courtyard refusals) — raise the piece cap or fall back to the convex cutter on the exterior's MRR frame."*

**How.**
1. In `_split_wings()` `:894`, raise the piece cap from `8` to a module constant `MAX_WING_PIECES = 14`. Keep the raise
   as a guard against runaway recursion, not as a target.
2. In `cut_wings()`, wrap the decomposition so that a `Refusal("WING_TREE_FAILED")` raised by `_split_wings()`,
   `_wing_tree()`, `_reduce_to_k_wings()` or `_alloc_k()` — **and only that token** — falls back to
   `cut_convex(poly, k, grp)` on the plate's own MRR frame, tagging the result `out["scheme"] = "wing_fallback_convex"`
   and `out["notes"]["fallback"] = "WING_TREE_FAILED"`. A refusal raised *by the fallback itself* propagates unchanged.
3. Do not touch the `:1249` refusal (circulation not a single hole-free Polygon) — that one is a real geometric failure,
   and a plate that reaches it has already been cut; it must stay a refusal.

**How to test.** A `python -c` harness, no pytest run: load the module with `_load_module`, take the eleven
representatives from `scripts/eu21/group_plans.json` plus the 29 `WING_TREE_FAILED` plates re-derived from the five
`rules_tests/test_0N.json` by `(district, building_id, drawn_per_floor)` through `01_cut_group_plans.py`'s
`load_universe()`, run `cut()` on each and report: how many now draw, how many still refuse and with which token, and for
every newly drawn plate `coverage`, `len(circulation.interiors)`, and whether any two zones overlap by > 0.02 m².
**Print counts, not geometries.** Expect: fewer than 29 refusals, 0 unexpected exceptions, coverage 0.999–1.001 on every
newly drawn plate. Report the number; do not tune to hit one.

---

### T02 — `T07` (a): one junction rectangle, stubs trimmed to it

**What.** At a wing junction the root band and the child bands are boxes in different frames, so their union is a stepped
outline that costs `C5` (point count / simple outline) and `C6` (2.50 m facade) on COMPLEX 11, L 4, U/T 3.

**Why.** Prompt §5 `T07` (a): *"wing junctions — the union of a root core and angled child stubs gives a stepped
circulation outline (`test05_U_OR_T_SHAPE.png` 29235, `test05_COMPLEX…` 31075, `way/432864641`): one junction rectangle
in the root frame, stubs trimmed to it."*

**How.**
1. For each child `j` with `parent[j] = p`, build **one junction rectangle** in the **parent's** frame, centred on
   `junctions[j]`, `CORRIDOR_W` across the parent's band direction and `CORRIDOR_W + LANDING` along it, clipped to `poly`.
2. Trim the child's own band so it starts at the boundary of that rectangle rather than overlapping into the parent —
   subtract the junction rectangle from the child band before the union, then add the rectangle back once.
3. Union order becomes `[core_world] + junction_rects + trimmed_band_geoms`. The existing `_bridge()` /
   band-growing fallback stays as the last resort and must not be removed.
4. After the union, apply the existing clean-up path unchanged. Do not add a new simplification pass.

**How to test.** Same harness. On the three named plates (`29235`, `31075`, `way/432864641`) and on all 21 `COMPLEX`
+ 10 `U/T` + 6 `L` FAIL plates: report per plate the circulation's **point count before and after**, whether `C5` and
`C6` would now pass (reuse `04_group_tests.py`'s own check functions by import — do not re-implement them), coverage, and
overlap. Redraw `EU-21/cutter_demo/demo_{L_SHAPE,U_OR_T_SHAPE,COMPLEX_MULTI_WING}.png` and **look at them** before
reporting. Expect the stepped outline gone; report any plate where it is not.

---

### T03 — `T07` (c): end the band at the cut when the next cell is a through-flat

**What.** `C4` costs 22 plates (COMPLEX 9, U/T 7, COURTYARD 2, SLAB 2, L 2): a flat cell wraps the end of a corridor and
erodes into two lobes.

**Why.** Prompt §5 `T07` (c): *"`C4` lobes on U/T (7) and COMPLEX (9): a flat cell wraps a corridor end — end the band at
the cut, not `+ LANDING`, when the next cell is a through-flat."*

**How.** In `cut_wings()` `:1170-1174` only:
- `u_far`: when the largest element of `reach` is a **cut** (`xs[-1] >= max(reach) - 1e-9`) **and** the cell beyond it is
  a through-flat (`M_w == 1`, or `odd_through` and it is the last cell), set `u_far = xs[-1]` — the band ends **on** the
  cut. The existing `odd_through` special case at `:1170-1171` becomes one branch of this rule, not a separate one.
  When the largest element of `reach` is a **child junction**, `+ LANDING` stays: the corridor has to reach into the child.
- `band_lo` `:1168`: apply the mirror rule at the low end — when `min(reach)` is a cut and the cell below it is a
  through-flat, `band_lo = xs[0]`.
- Leave `:1163` (the `DD-8` one-flat leaf stub) exactly as it is.
- Do **not** touch `cut_convex()` `:364-365`; the convex groups pass 50/50 and are out of scope.

**How to test.** Same harness, over all 22 `C4` plates plus the seven convex representatives at `k ∈ {2,3,4,5,6,7,8,9,12}`
as a regression. Report: `C4` pass/fail per plate using `04`'s own check, coverage, and any plate that flipped from PASS
to FAIL or from drawn to refused. A convex regression that changes any of the 63 outcomes is a **stop-and-report**.

---

### 🔴 CP-1 — after T03. Report to the director.

Report: for each of T01/T02/T03 the counts asked for above, the new line count of `05_group_cutters.py`, every deviation,
the debug-reference bullets added, and any plate that got worse.

---

### T03b — `DD-10` withdraw the junction rectangle; `DD-11` extend the fallback to `len(wings) < 2`

**What.** Two edits to `scripts/eu21/05_group_cutters.py`, and nothing else in the repo.

**Why.** `CP-1` measured T02 (the junction rectangle) as net-negative on its own acceptance
criterion: the circulation point count **rose** on 29 of 39 measured plates, the `C5`/`C6` gains
were partial (19/39 and 25/39), the 3-wing stepped outline was visibly unchanged, and two
previously-drawn plates — `IT-BOL-GALVANI2/29235` (this plan's own named example) and
`ES-MAD-BERRUGUETE/way/968439455` — became outright refusals. A rule that fails its own
acceptance criterion is withdrawn, not patched. Recorded as `FINDING 225`.
Separately, T01's fallback reached only 5 of the 29 `WING_TREE_FAILED` plates because 11 more
refuse at `cut_wings()`'s own `len(wings) < 2` check, which T01 did not name. A plate whose
decomposition yields fewer than two wings is not a wing shape at all, so `cut_convex()` is its
honest handler — the same reasoning that justified the fallback in the first place.

**How.**

1. **`DD-10` — revert T02 exactly, by these hunks and no others:**
   - delete the whole `# T07 (a): one junction rectangle per child …` block — `junction_rects = []`,
     `half_along` / `half_across`, the `for j, p in parent.items()` loop, the `rect_world` build and
     the `band_geoms[j] = set_precision(band_geoms[j].difference(rect_world), 0.001)` trim;
   - both `unary_union([core_world] + junction_rects + band_geoms)` expressions revert to
     `unary_union([core_world] + band_geoms)`;
   - leave every T01 and T03 line in place. Verify with `git diff` that nothing from T01 or T03 moved.
2. **`DD-11`** — in `cut_wings()`, replace
   `if len(wings) < 2: raise Refusal("WING_TREE_FAILED")` with
   `if len(wings) < 2: return _wing_fallback_convex(poly, k, grp)`.
   Do **not** touch the final circulation check (`circ_world.geom_type != "Polygon" or interiors`):
   that one stays a refusal, as T01 specified.

**How to test.** Re-run T01's own 29-plate harness: report how many now draw, by which route
(raised cap / `len(wings) < 2` fallback / other), and coverage + `len(interiors)` + max pairwise
overlap on each newly-drawn plate. Re-run T02's 39-plate harness and confirm `29235` and
`way/968439455` draw again. Re-run the 63-case convex regression and confirm **0 outcomes changed**
against `git show HEAD:scripts/eu21/05_group_cutters.py`. No `04` run, no `03` run.

### T04 — re-measure the 550 plates and regenerate the five test sheets

**What.** Run the full test battery on the fixed cutter and rebuild the five `TEST_0N` sheets and the 55 contact sheets.

**Why.** The plan's own rule: a fix is worth what it measures. The sheets are the owner's deliverable.

**How.**
1. **First** preserve the current results: copy `test_0N.json` → `test_0N.t06.json` for `N = 1..5` (copy, do not move — `04`
   overwrites in place). The existing `.cap8`, `.cap12` and `.postpass` files are **not** touched.
2. `.venv\Scripts\python.exe scripts\eu21\04_group_tests.py --test N` for `N = 1,2,3,4,5`, one at a time, foreground.
3. Fix the `T07` (e) cosmetic regression **while you are there and only there**: the contact-sheet titles lost the scheme
   name in T06c. Restore it in the title string only. This is the single exception to "only the file named in the task".
4. Report the tally: total PASS / FAIL / REFUSED / errors, the per-group table, the failing-check table and the
   refusal-token table, in the same shape as §3, so the two can be read side by side.

**How to test.** The run is the test. Additionally open `rules/tests/archive/TEST_05_ten_per_group_2026-09-01.html` and confirm it
renders and that the `C8` chip and the lede are intact.

---

### 🔴 CP-2 — after T04. Report to the director.

Report the two tables against §3.

---

### T05 — the `03` run: bring the RULES document level with the code

**What.** Regenerate `rules/RULES_dwelling_layout_groups_2026-09-01.html` so its text describes the direct cutter.

**Why.** Ordered by the owner 2026-09-02. Prompt §5: *"sheet 01 step 3 (cores + open gallery), sheet 07 step 3–4
(incentre wedges → convex cutter), sheet 10 step 3 (arm refusal → corridor), sheets 03–06 step 4 at k ≥ 5
(`ceil(k/M)×M` + corridor)."*

**How.** Edit only `scripts/eu21/03_build_rules_html.py`, keeping every bullet **one line**:
- **Sheet 01 `COURTYARD`, `core`** — `D-EU-67`: corner stair cores joined to each other into one circulation zone, plus
  the open gallery; a void under 6 m is not a courtyard at all (`DD-7`) and the plate is cut by its exterior's group with
  the void on a party wall. Set `status` to match reality.
- **Sheet 07 `TRIANGLE`, `core` + `cut`** — `DD-2`: the convex cutter (equal-area columns in the MRR frame, straight row
  line through the centroid, core at the centroid), not incentre wedges. Check **sheet 08 `TRAPEZOID`** for the same
  wording and correct it if it is there — report either way.
- **Sheet 10 `U_OR_T_SHAPE`, `core`** — `DD-5`/`DD-8`: a corridor reaches into a long arm instead of the arm being
  refused; it spans only from the first served cut or junction to the last, and a one-flat leaf arm gets a 1.5 m stub.
- **Sheets 03–06 (`SQUARE`, `RECTANGLE`, `CORRIDOR_RECTANGLE`, `SLAB`), `cut`** — `DD-3`: at k ≥ 5, `ceil(k/M)` equal-area
  columns × M rows with a straight corridor; `DD-6`: M ∈ {1,2} chosen by the smaller max aspect ratio with a 3.0 m width
  floor on every flat, and `BAND_LT_3M` when no loading is valid.
- **Sheets 09 and 11 (`L_SHAPE`, `COMPLEX_MULTI_WING`), `core` + `cut`** — the same wing rules as sheet 10, plus T01's
  convex fallback if it fired for that group.
- `ABSORB` and `CLOSE` are the law and are **shared** — change them only if T01–T03 changed the law itself, which they
  do not. Leave them.
- **`DD-9`:** add a module constant `USE_CUTTER = True`. When true, the eleven representative plans are cut by
  `05_group_cutters.py` and written to a **new** `scripts/eu21/group_plans_cutter.json`; `scripts/eu21/group_plans.json`
  is read-only in this task and must not change on disk (verify by hash before and after). When false, the old file is
  used and only the text changes.

**How to test.** `sha256` the HTML before and after; report which sheets changed and the byte delta. Render the file in
headless Chrome, screenshot sheets 01, 07 and 10, and **look at them**: two figures per sheet, seven numbered steps,
steps 5 and 6 marked shared. Confirm `scripts/eu21/group_plans.json` has the same hash as before the task.

---

### 🔴 CP-3 — after T05. The plan closes here.

---

## 7. Stop-and-report points

`CP-1` after T03 (the three cutter fixes) · `CP-2` after T04 (the re-measure) · `CP-3` after T05 (the document).
A convex-regression change in T03 is an immediate stop, before CP-1.

---

## 8. Progress log

#### T01 — `T07`(b) piece cap + convex fallback — completed 2026-09-02

**Artifacts.** `scripts/eu21/05_group_cutters.py`: `MAX_WING_PIECES = 14` constant added; `_split_wings()`'s two `> 8` checks now read `> MAX_WING_PIECES`; new `_wing_fallback_convex(poly, k, grp)` helper; `cut_wings()` gained a `grp` parameter and three `try/except Refusal` blocks around the calls to `_split_wings()` (+`_absorb_bays`/`_reclaim_denoise_loss`), `_reduce_to_k_wings()`/`_wing_tree()`, and `_alloc_k()` — a caught `WING_TREE_FAILED` (and only that token) falls back to `cut_convex(poly, k, grp)` on the plate's own MRR frame, tagged `scheme="wing_fallback_convex"`, `notes["fallback"]="WING_TREE_FAILED"`; any other token, or a refusal from the fallback itself, propagates. Both call sites (`_dispatch_group`, `cut`) updated to pass `grp`.

**Deviations.**
1. The `if len(wings) < 2: raise Refusal("WING_TREE_FAILED")` check inside `cut_wings()` (plan §4's `:1097`) was **not** wrapped in the fallback — it is raised directly by `cut_wings()`, not by one of the four named functions, and plan §4 names it as a distinct site (separate from `:1249`) without including it in T01 §6 step 2's list, unlike `:1249` which is explicitly excluded with a reason. Measured effect: 11 of the 29 target plates fail here (all 6 `L_SHAPE`, 2 `COURTYARD`, 2 `COMPLEX_MULTI_WING`, 1 `U_OR_T_SHAPE`) and stay refused. Flagged for the director, not fixed unilaterally — see Notes.
2. Both `> 8` checks inside `_split_wings()` were raised to `MAX_WING_PIECES`, not only the one at (old) `:894` the plan cites — the final `if len(finished) > 8` check enforces the same cap and would otherwise still refuse every plate the raised cap was meant to admit.

**Test status.** Own `python -c` harness (no pytest, no `04`/`03`/`01`/`02` run as scripts), scratchpad only. 29 `WING_TREE_FAILED` plates re-derived from the five `test_0N.json` by `(district, building_id, drawn_per_floor)`, geometry via `01_cut_group_plans.py`'s `load_universe()`, polygon centred exactly as `04_group_tests.py`'s `centred()` does (confirmed required — 3 of the 29 draw differently at plate-native UTM coordinates vs. centred, a `set_precision`/coordinate-magnitude sensitivity, not part of this fix). Result: **5 of 29 now draw** (4 `COURTYARD` light-well reroutes, 1 `COMPLEX_MULTI_WING`), all via the raised piece cap alone — the convex fallback never fired for any of the 29 (0/29). 24 still refuse `WING_TREE_FAILED`: 11 at the untouched `len(wings)<2` check (Deviation 1), 6 at the untouched `:1249`-equivalent circulation check (protected by design), 4 at `cut_courtyard`'s own refusal sites (2 real courtyards, out of `cut_wings`'s reach), 2 `SLAB` at `cut_convex`'s own bridging refusal (DD-7 reroute dispatches to `cut_convex`, not `cut_wings`, out of scope). On the 5 newly-drawn plates: coverage 1.0000 on every one, `len(circulation.interiors) == 0` on every one, max pairwise zone overlap 0.0000 m² on every one. 0 exceptions. Regression: all 11 `group_plans.json` representatives × k∈{2,3,6,9,12} (55 cases) still draw or refuse honestly (`BAND_LT_3M`), 0 exceptions, 0 `WING_TREE_FAILED`.

**Notes.** The piece-cap raise + fallback only reaches 5 of the 29 plates the prompt's diagnosis targeted (12 light-well courtyards). Most of the remainder fail at the separate `len(wings)<2` site, which plan §6 T01 did not name for the fallback. Whether to extend the fallback to that site is a director decision, not taken here (no alternative proposed, plan executed as written).

#### T02 — `T07`(a) junction rectangle, stubs trimmed to it — completed 2026-09-02

**Artifacts.** `scripts/eu21/05_group_cutters.py`, `cut_wings()`: new block after the per-wing band loop builds one junction rectangle per child `j` (`parent[j]=p`) in the parent's own frame, centred on `junctions[j]`, `CORRIDOR_W` across × `CORRIDOR_W+LANDING` along, grown 0.05 m before the `poly` clip (bug fix, see Deviation 1), then `band_geoms[j]` is trimmed by that rectangle before the union. Both `combined = unary_union([core_world] + band_geoms)` expressions (first computation and the regrowth-loop recomputation) became `unary_union([core_world] + junction_rects + band_geoms)`; no other line in the clean-up path touched. Redrew `EU-21/cutter_demo/demo_{L_SHAPE,U_OR_T_SHAPE,COMPLEX_MULTI_WING}.png`.

**Deviations.**
1. **Bug found and fixed, in scope.** The literal construction (rectangle clipped to `poly` with no growth) produced a hairline interior-ring sliver in `combined` on some plates via independent `set_precision` rounding in two different rotated frames (see debug-reference ch.1 entry added this task), turning a plate that drew cleanly before T02 into a `WING_TREE_FAILED` refusal at the untouched, protected final check. Fixed by growing the rectangle 0.05 m (same tolerance `_junction()` already uses) before the `poly` clip. This is a robustness correction to the specified construction, not a change to its shape, position or the algorithm's law.
2. Tested a second hypothesis (centring the rectangle's y on the parent band's own cross-section instead of the raw junction point's y) to see whether it explained the aggregate result below; it did not measurably change the outcome (still 2/39 refused, still ~35/39 pts-up), so it was **not** kept — the shipped code centres literally on `junctions[j]` as plan §6 T02 step 1 specifies.

**Test status.** Same harness style. Three named plates (`29235`, `31075`, `way/432864641`) plus all 37 raw `FAIL`-verdict records in `COMPLEX_MULTI_WING`/`U_OR_T_SHAPE`/`L_SHAPE` from the five `test_0N.json` (39 rows total, the three named plates included since `31075`/`way/432864641` are `PASS`-verdict and not already among the 37). Reused `04_group_tests.py`'s own `run_checks()` by import (not re-implemented) for `C5`/`C6`. Circulation-only point count (not `C5`'s all-zones point count) computed from each plate's own `circulation` field before, and from a fresh `cut()` call after. Result: **circulation point count rose on 29 of 39 rows** (fell on 2, unchanged on 2, 6 no-longer-comparable due to refusal); `C5` now passes on 19/39, `C6` on 25/39, **both on 13/39**; **2 of 39 plates got worse to an outright refusal** — `U_OR_T_SHAPE`/`IT-BOL-GALVANI2/29235` (one of the three plan-named plates) and `COMPLEX_MULTI_WING`/`ES-MAD-BERRUGUETE/way/968439455`, both at the untouched, protected final circulation check; both traced (traceback line + direct inspection) to a real coverage gap in `combined`, not the sliver bug (0.157 m² and 0.00016 m² respectively). Demo PNGs viewed: the two-wing `L_SHAPE`/`COURTYARD`-style junction is visibly smoother (isolated debug plot confirmed a clean rectangle, no stepped crossing) but a small residual 2-point notch remains at the child-band/rectangle seam; the 3-wing `U_OR_T_SHAPE` (two children on one root) shows the stepped/zig-zag circulation **still present**, materially unchanged from before T02. 0 exceptions.

**Notes.** T07(a) as specified (one rectangle per child, parent's own frame, child band trimmed) works as intended for an isolated 2-wing junction, but on real plates with 2+ children on the same root, or a chain of junctions, it does not eliminate the stepped outline and net-increases circulation complexity on most measured plates, including 2 new outright refusals (one the plan's own named example). Registered `[OPEN]` in the debug-reference file with root-cause analysis (uncovered space between independently-built rectangles / between a rectangle and the parent's own untrimmed reach). Executed exactly as written — no alternative algorithm proposed or substituted; flagged for the director's decision.

#### T03 — `T07`(c) band ends on the cut when the next cell is a through-flat — completed 2026-09-02

**Artifacts.** `scripts/eu21/05_group_cutters.py`, `cut_wings()`, the `else` branch after the `k_per_wing[i]==1 and not children[i]` (DD-8 stub) branch only. Added `first_is_through_flat = (M_w==1) or (odd_through and N_w==1)` and `last_is_through_flat = (M_w==1) or odd_through`. `u_far`: `xs[-1]` when `last_is_through_flat` and the largest `reach` element is a cut (`xs[-1] >= max(reach)-1e-9`), else unchanged `(max(reach)+LANDING)`. `band_lo` (root wing only, mirrored): `xs[0]` when `first_is_through_flat` and the smallest `reach` element is a cut (`xs[0] <= min(reach)+1e-9`), else unchanged `(min(reach)-LANDING)`. Line `:1163` (DD-8 stub) and `cut_convex()` `:364-365` untouched, per plan.

**Deviations.** None from the written rule. The `odd_through`-derived "next cell is a through-flat" condition needed a small, non-optional generalisation to correctly mirror at the low end: `odd_through` only special-cases the **last** column (`ci==N_w-1`); the low-end mirror of "is the cell beyond it a through-flat" is only true via that same special case when the wing has exactly one column (`N_w==1`, so column 0 *is* the last column) — hence `first_is_through_flat` includes `odd_through and N_w==1`, not bare `odd_through`. This is the literal mirror of the stated rule, not a new rule.

**Test status.** Same harness style. 22 `C4`-failing plates re-derived from the five `test_0N.json` (`verdict=="FAIL" and checks.C4.pass==False`) — group split (`COMPLEX_MULTI_WING` 9, `U_OR_T_SHAPE` 7, `COURTYARD` 2, `SLAB` 2, `L_SHAPE` 2) matches plan §3 exactly. Result: **1 of 22 plates now passes `C4`** (`COMPLEX_MULTI_WING`/`way/311159688` k=9, counted twice in the raw 22 — same plate in two test files); the `COURTYARD`(2)/`SLAB`(2) failures are unreachable from `cut_wings()` by design (different functions, out of scope) and stayed `False`; `U_OR_T_SHAPE`/`29235` k=3 stayed refused (the T02 regression above, not new). Diagnosed the low yield directly: most of the 18 in-scope (`COMPLEX_MULTI_WING`+`U_OR_T_SHAPE`+`L_SHAPE`) `C4` failures are on wings with `k_per_wing<=2` and `N_w<=1`, where `xs` is empty and this rule's guard (`xs and reach and ...`) never fires — the lobe on those plates forms at the wing junction itself (T02's territory), not from a `+LANDING` overshoot past an internal cut, so this fix's own trigger condition does not apply to them. 0 exceptions. **Convex regression (63 cases, 7 groups `SLIVER,SQUARE,RECTANGLE,CORRIDOR_RECTANGLE,SLAB,TRIANGLE,TRAPEZOID` × k∈{2,3,4,5,6,7,8,9,12}): 0 of 63 outcomes changed** between the original (`git show HEAD:scripts/eu21/05_group_cutters.py`) and the T01–T03 code on the same `group_plans.json` representatives — not a stop-and-report. Measured absolute counts on the current (already-modified, pre-existing `M` in git status, not touched by this task) `group_plans.json`: 52 drawn / 11 `BAND_LT_3M` / 0 errors, not the plan's quoted 51/12 — attributed to `group_plans.json` differing from whatever generated that quoted baseline, not to any T01–T03 change (proven by the 0-changed-outcomes diff against the unmodified cutter on the identical file).

**Notes.** Fix implemented exactly as specified; its own scope (wings with `N_w>=2`, i.e. an internal cut for the band to overshoot past) does not cover most of the 22 sampled real `C4` failures, which form at wing junctions instead. Honest result, not tuned.

#### T03b — `DD-10` withdraw junction rectangle, `DD-11` extend fallback to `len(wings)<2` — completed 2026-09-02

**Artifacts.** `scripts/eu21/05_group_cutters.py` only. `DD-10`: deleted the whole `# T07 (a): one junction rectangle per child …` block (`junction_rects = []`, `half_along`/`half_across`, the `for j, p in parent.items()` loop, the `rect_world` build and the `band_geoms[j] = set_precision(band_geoms[j].difference(rect_world), 0.001)` trim) and both `unary_union([core_world] + junction_rects + band_geoms)` expressions reverted to `unary_union([core_world] + band_geoms)`. `DD-11`: `if len(wings) < 2: raise Refusal("WING_TREE_FAILED")` → `if len(wings) < 2: return _wing_fallback_convex(poly, k, grp)`. No other line touched; `grep -c junction_rects` = 0; `git diff --stat` (whole uncommitted session, T01+T02+T03+T03b) = 58 insertions / 19 deletions; file is 1466 lines (was 1494 before this task, 1427 at `HEAD`).

**Deviations.** None — both edits exactly as specified in plan §6 T03b step "How".

**Test status.** Own `python -c` harness, scratchpad only (no `04`/`03`/`01`/`02` run as scripts). (1) T01's 29-plate `WING_TREE_FAILED` pool (raw rows, not deduped, same method as T01): **16 of 29 now draw** (up from 5 before `DD-11`) — route split (via a monkeypatched `_wing_fallback_convex` classifying the caller line number): 7 via the raised piece cap (direct wing decomposition, no fallback), 9 via the new `len(wings)<2` fallback (`DD-11`), 0 via the other three fallback sites. 13 raw rows (7 unique plates) still refuse, all token `WING_TREE_FAILED`: 7 raw rows (4 unique plates: `IT-BOL-GALVANI2/28782`, `FR-LYO-HAUTCOEURPENTES/BATIMENT…156`, `IT-BOL-GALVANI2/30390`, `IT-BOL-GALVANI2/32211`) at `cut_wings()`'s own protected final circulation check (untouched, by design); 4 raw rows (2 unique `COURTYARD` plates, `31528`/`28125`) at `cut_courtyard()`'s own refusal sites, unreached by `cut_wings`; 2 raw rows (1 unique `SLAB` plate, `28583`) at `cut_convex()`'s own `WING_TREE_FAILED` raise, also outside `cut_wings`. On all 16 newly-drawn rows: coverage 100.0 % (`C1` pass) on every one, `len(circulation.interiors) == 0` on every one, max pairwise zone overlap 0.0000–0.0045 m² (all < the `C4` 0.02 m² threshold). 0 exceptions. (2) T02's 39-plate harness (37 raw `FAIL`-verdict rows in `COMPLEX_MULTI_WING`/`U_OR_T_SHAPE`/`L_SHAPE` + the 2 named rows not already in that 37): **all 39 draw, 0 refused**, including the two T02 casualties — `IT-BOL-GALVANI2/29235` and `ES-MAD-BERRUGUETE/way/968439455` both draw again (`wing_spine_decomposition`). Circulation point count vs. the pre-T01/T02/T03 baseline in `test_0N.json`: **25/39 unchanged, 10/39 down, 4/39 up, 0 refused** — most of the 39 are back at baseline exactly (T02 undone), the 10 "down" are T03's own band-end-at-cut effect (still active, correctly independent of T02), the 4 "up" likewise trace to T01/T03, not to any surviving T02 code. (3) 63-case convex regression (7 groups × k∈{2,3,4,5,6,7,8,9,12}) against `git show HEAD:scripts/eu21/05_group_cutters.py`: **0 of 63 outcomes changed** (stop-and-report condition not triggered). Absolute counts on the current code: 52 drawn / 11 `BAND_LT_3M` / 0 errors — identical to T03's own measurement, as expected since `DD-10`/`DD-11` touch neither `cut_convex` nor the convex dispatch path.

**Notes.** `DD-10` restores the pre-T02 circulation shape on 25 of the 39 T02-regression plates exactly and recovers both named casualties; `DD-11` reaches 9 more of the original 29 `WING_TREE_FAILED` plates beyond T01's 5, for 16/29 total now drawing. No new error class encountered; no debug-reference entry added. Executed exactly as written — no alternative proposed.

#### T04 — re-measure the 550 plates — completed 2026-09-02

**Artifacts.** The five copies made first: `openubem/outputs/eu_evidence/EU-21/rules_tests/test_0{1..5}.t06.json` (pre-T04 results preserved, `.cap8`/`.cap12`/`.postpass` untouched). `scripts/eu21/04_group_tests.py:110` — the one allowed edit — `aria-label="{TITLE[rec['group']]} floor plan"` → `aria-label="{TITLE[rec['group']]} floor plan, {rec['scheme']} scheme"` (`T07`(e) fix, see debug-reference ch.1). `.venv\Scripts\python.exe scripts\eu21\04_group_tests.py --test N` run for N=1..5 in order, each foreground, each overwriting `test_0N.json` and `rules/tests/TEST_0N_*.html` in place; 0 exceptions across all five runs.

**Tally (550 plates, T01–T03b cutter): 440 PASS / 54 FAIL / 56 REFUSED / 0 errors** — §3 baseline was 432/50/68.

Per group (PASS/FAIL/REFUSED), §3 baseline in parentheses: `SQUARE` `RECTANGLE` `TRIANGLE` `TRAPEZOID` 50/0/0 (unchanged) · `CORRIDOR_RECTANGLE` 46/0/4 (unchanged) · `SLAB` 43/4/3 (unchanged) · `SLIVER` 38/0/12 (unchanged) · `L_SHAPE` 39/6/5 (35/6/9) · `U_OR_T_SHAPE` 31/10/9 (32/10/8) · `COMPLEX_MULTI_WING` 26/21/3 (23/21/6) · `COURTYARD` 17/13/20 (15/9/26).

Failing checks over the 54 FAIL plates, §3 in parentheses: `C6` 22 (18) · `C4` 22 (22) · `C5` 19 (17) · `C2` 5 (5).

Refusal tokens over the 56 REFUSED plates, §3 in parentheses: `BAND_LT_3M` 30 (30) · `WING_TREE_FAILED` 17 (29) · `FLAT_ENCLOSES_ZONE` 6 (6) · `CELL_EMPTY` 3 (3).

**Deviations.** None from the written steps. Order of operations followed exactly (copy first, edit title string, run five tests in sequence, inspect `TEST_05`).

**Test status.** The five `04` runs are the test; all five completed with exit 0, `wrote ... .json` / `wrote ... .html` confirmed for each. Diffed every one of the 550 rows against its own `.t06.json` predecessor by `(test_n, group, district, building_id, drawn_per_floor)`: 530/550 unchanged, 16 improved (6 `COURTYARD`, 3 `COMPLEX_MULTI_WING`, 6 `L_SHAPE`, 1 `U_OR_T_SHAPE` — all `REFUSED`→drawn), **4 rows (2 distinct plates) got worse, both `PASS`→`REFUSED("WING_TREE_FAILED")` at k=6: `L_SHAPE`/`ES-MAD-BERRUGUETE/way/432412736` and `U_OR_T_SHAPE`/`IT-BOL-GALVANI2/33363`**, each appearing in both `test_02` and `test_04` (same physical plate selected in both size-bucket tests). Not fixed — a measurement task does not fix what it measures; reported as found. `rules/tests/archive/TEST_05_ten_per_group_2026-09-01.html` opened: renders (closes with `</html>`, 368916 bytes), 104 `C8` chips present, `<p class="lede">` intact and non-empty. `aria-label` spot-checked across the file: scheme name (including `+lightwell` suffixes) now present on every drawn plate's SVG.

**Notes.** Net effect of T01–T03b on the full 550-plate census: PASS +8, FAIL +4, REFUSED −12, 0 errors throughout. `WING_TREE_FAILED` fell from 29 to 17 (T01's piece-cap raise + `DD-11`'s `len(wings)<2` fallback reaching plates T01 alone could not). `C6`/`C5` rose slightly (18→22, 17→19) and `C4` held exactly at 22 — consistent with T03's own T03 finding that most `C4` failures form at wing junctions, outside that fix's trigger condition, and with `DD-10` having withdrawn T02's `C5`/`C6` attempt. The 2 new `WING_TREE_FAILED` regressions are a genuine, unresolved side effect of `DD-11` (or the interaction of the raised piece cap with it) on these two specific k=6 plates — flagged for the director, not chased.

#### CP-2 — director audit of T04 — signed 2026-09-02

**Verified independently** (director's own tally over the five `test_0N.json` against the five `.t06.json`
snapshots, not the executor's numbers): PASS 440 / FAIL 54 / REFUSED 56 / 0 errors of 550, against §3's
432 / 50 / 68. Failing checks `C6` 22 (18) · `C4` 22 (22) · `C5` 19 (17) · `C2` 5 (5). Tokens
`BAND_LT_3M` 30 (30) · `WING_TREE_FAILED` 17 (29) · `FLAT_ENCLOSES_ZONE` 6 (6) · `CELL_EMPTY` 3 (3).
Every figure matches the executor's report exactly.

**Plate-level diff, old → new:** 16 rows improved (all `REFUSED` → drawn: 11 `PASS`, 5 `FAIL` — the five
being `COURTYARD` rows that now draw but miss a quality check), 4 rows worsened (`PASS` →
`REFUSED(WING_TREE_FAILED)`), those 4 being **two distinct buildings** each selected in both `test_02` and
`test_04`: `L_SHAPE / ES-MAD-BERRUGUETE / way/432412736` (k=6, own_k 4) and
`U_OR_T_SHAPE / IT-BOL-GALVANI2 / 33363` (k=6, own_k 3). Net −12 refusals.

**Reading.** The `C6`/`C5` rise is not a regression in the rules: it is the arithmetic of 12 plates that
previously produced no drawing at all now producing one that is checked. No plate that already drew got a
worse check result. `C4` held exactly, confirming T03's own finding that most `C4` lobes form at wing
junctions, outside its trigger.

**`DD-12` (director, unratified).** The two k=6 regressions are accepted, not chased: `DD-11` buys 16 rows
and costs 4, and the plan's own rule is that a measurement task does not fix what it measures. Recorded as
`FINDING 226`. If the owner wants them recovered it is a separate task, after `CP-3`.

**Verdict: CP-2 passed.** Only `04_group_tests.py:110` (the `rec["scheme"]` restoration the plan authorised
as the single exception), the five `test_0N.json`, the five `TEST_0N…html` sheets and the plan's own log were
touched; the five `.t06.json` snapshots exist as copies, the `.cap8` / `.cap12` / `.postpass` siblings are
untouched. T05 dispatched.

#### T05 — the `03` run — completed 2026-09-02

**Artifacts.** `scripts/eu21/03_build_rules_html.py` only (650&rarr;754 lines). SPEC text rewritten on 9 of 11
sheets: `COURTYARD` (`status` "proposed"&rarr;"in-force", `core` D-EU-67 joined corner cores + DD-7 void&lt;6&nbsp;m
reroute), `TRIANGLE` (`core`/`cut` DD-2 convex cutter, not incentre wedges), `SQUARE`/`RECTANGLE`/
`CORRIDOR_RECTANGLE`/`SLAB` (`cut` DD-3 `ceil(k/M)`&times;M + DD-6 M-choice/`BAND_LT_3M`; the latter two also
corrected off a stale "single-loaded below 12&nbsp;m, double-loaded above" claim to their real forced M=1),
`L_SHAPE`/`U_OR_T_SHAPE`/`COMPLEX_MULTI_WING` (`core` DD-5/DD-8 corridor-into-arm + first-to-last span + 1.5&nbsp;m
stub; 09/11 `cut` gains the `MAX_WING_PIECES=14` / `&lt;2`-wings convex-fallback line). `SLIVER`(02)/`TRAPEZOID`(08)
SPEC bullets untouched (08 checked for the incentre-wedge wording named alongside sheet 07 &mdash; not present).
`ABSORB`/`CLOSE` untouched. `DD-9`: `USE_CUTTER = True` (`:361`), `build_cutter_plans()` (`:373-451`) loads
`01_cut_group_plans.py`/`05_group_cutters.py` by path, re-derives each of the 11 representative plates the way
`01`'s own `main()` does (representative-first, closest-to-`n_draw` fallback over &le;60 candidates) but calls
`m05.cut()` instead of the old engine, writes `scripts/eu21/group_plans_cutter.json` (new file); `group_plans.json`
is only read, and only when `USE_CUTTER = False`.

**Deviations.**
1. `CORRIDOR_RECTANGLE`/`SLAB` drawn at `n_draw=5` inside `build_cutter_plans()`, not `01`'s `MIN_DRAW_TO_SHOW_SCHEME=3`:
   under the direct cutter the corridor-band scheme (DD-3) only appears at k&nbsp;&ge;&nbsp;5 (`cut_convex()` `:306`
   `if k&lt;=4: ... else: ...`); at k=3 both groups drew the same plain box-core branch as `SQUARE`, contradicting
   their own corrected text. A local `CUTTER_MIN_DRAW` dict overrides the count for these two groups only;
   `01_cut_group_plans.py` untouched. Not ordered by the plan; done because DD-9's own purpose (picture matches
   text) would otherwise fail on these two sheets. Flagged for the director.
2. Added a candidate tie-break (rank 0/1/2) in `build_cutter_plans()`: without it, `COURTYARD`'s actual
   representative building draws only via a DD-7 light-well reroute (`wing_spine_decomposition+lightwell`), showing
   a different group's algorithm under the "Courtyard" title against the D-EU-67 text just written. Preferring a
   same-scheme draw among the &le;60 candidates picks `ES-MAD-BERRUGUETE/relation/12713026`
   (`courtyard_cores_gallery`) instead; it also changed which building `U_OR_T_SHAPE`/`COMPLEX_MULTI_WING` draw, for
   the same reason. Not ordered by the plan; same reasoning as (1).
3. `COMPLEX_MULTI_WING`'s `cut` first bullet was rewritten beyond the ordered fallback line: the pre-existing "split
   by morphological opening, `buffer(&minus;d).buffer(+d)` at d=4.0&nbsp;m" sentence describes the old engine, not
   `_split_wings()`/`_split_once()` (reflex-vertex splitting, plan &sect;4's own citation), and would otherwise sit
   beside the new fallback sentence in the same list. Corrected to describe the real method.
4. Sheet 10's `refuse` ("An arm longer than 15&nbsp;m&hellip;") and sheet 11's `refuse` ("Opening at d=4.0&nbsp;m
   yields no plate") were left untouched (plan scoped those sheets to `core`/`cut` only) though both are now stale
   against the code (no 15&nbsp;m arm-length refusal exists; `_split_wings()` does not buffer-open). Reported, not
   fixed. Sheet 07's own `refuse` ("the tip wedge below 6&nbsp;m&sup2;") is likewise stale ("tip wedge" no longer
   exists under `cut_convex()`) and likewise left untouched.
5. Pre-T05 HTML byte size was not captured before the first `03` run (only its sha256) &mdash; the byte delta below
   is therefore incomplete. Reported as found, not reconstructed.

**Test status.** No pytest; own inspection. sha256 before `D2770AA1CFB64A5DB6BF545242D5327C33DB677B009F259FEADEB9BD4CAAA7A0`
(byte size not captured, Deviation 5) &rarr; after `B3731BD96BAC2D12671D2EED567DCC82E9EF68A1EFDBDFD051B665CABB9F4B73`,
130964 bytes. Every group's `circ_m2` differs old vs. new cutter output &mdash; including the 5 sheets whose scheme
label and representative building are unchanged (`SQUARE`/`RECTANGLE`/`TRIANGLE`/`TRAPEZOID`/`SLAB`), because the
cutting algorithm itself differs from the old engine &mdash; so all 11 sheets' drawings changed; SPEC bullet text
changed on 9/11 (all but `SLIVER`/`TRAPEZOID`). No headless browser available (`playwright`/`selenium` not
installed, no `chrome.exe`/`msedge.exe` on PATH) &mdash; verified structurally instead: 11 `&lt;article
class="sheet"&gt;`, 22 `&lt;svg class="plan"&gt;` (2/sheet); sheets 01/07/10 (`COURTYARD`/`TRIANGLE`/`U_OR_T_SHAPE`)
each have exactly 2 `.fig`, 7 `.fstep`, 2 `.fstep.shared` (steps 5/6), non-trivial `&lt;path&gt;` counts (7/6/7).
`group_plans.json` sha256 `F4F2829E9A88A16DE31437A66DE61B08C61F160DA25358EE840AF39DF36F7BAF` identical before and
after (checked twice). `group_plans_cutter.json` written: 11 plans, 0 refused.

**Notes.** `DD-9` implemented as `USE_CUTTER = True`; flipping to `False` reverts `PLANS` to reading
`group_plans.json` unchanged, per the plan's own escape hatch. `refuse`-field staleness on sheets 07/10/11
(Deviation 4) is reported, not chased, consistent with T01&ndash;T04's own precedent. Executed as written except
where Deviations 1&ndash;3 name a specific, narrowly-scoped correction needed for DD-9's own stated purpose (the
picture must match the sheet's own text); no other alternative proposed.

#### CP-3 — after T05. The plan closes here — reached 2026-09-02, awaiting director sign-off.

**Director audit — signed 2026-09-02.**

**Verified independently** (director's own commands, not the executor's numbers):
`scripts/eu21/group_plans.json` sha256 `f4f2829e…`, mtime 16:59 of the previous day — not opened during the
T05 window at all, so `DD-9` held. Files written at 09:22–09:25: `03_build_rules_html.py`,
`RULES_dwelling_layout_groups_2026-09-01.html` (130,964 B, sha256 `b3731bd9…`), the new
`group_plans_cutter.json` (11 plans, all drawn), the plan's own log and the debug references.
`05_group_cutters.py` (08:48) and `04_group_tests.py` (08:59) both predate T05 — no cutter or test-harness
edit crept in. Structure re-counted by hand: 11 `sheet` articles, 22 `svg.plan`, `SLIVER` `circ_m2` = 0.0
(zero cores, `D-EU-64`). The only surviving `incentre`/`wedge` strings in the whole document are sheet 07's
**new** wording (“centroid, not the incentre — the convex cutter has no wedge geometry to centre on”, “not
wedges radiating from a corner”); the stale claim is gone. The debug entry is present and in house format.

**Director's own finding, not in the executor's report.** Deviations 1 and 2 together change **which building
4 of the 11 sheets draw**: `COURTYARD` 29530 → `relation/12713026`, `CORRIDOR_RECTANGLE` `way/322742247` →
30800, `U_OR_T_SHAPE` `way/420411321` → `way/391276758`, `COMPLEX_MULTI_WING` `way/310771778` →
`way/310738155`. Three come from the same-scheme tie-break, one from `CUTTER_MIN_DRAW` moving the draw from
k=3 to k=5. Two further sheets keep their building but change scheme under the cutter itself
(`SLIVER` → `row_house_depth_bands`, `L_SHAPE` → `l_shape_decomposition`), which is `DD-9` working as
intended. Each sheet names its building id and its scheme in the document, so every substitution is visible
to a reader; that disclosure is what makes the tie-break admissible rather than flattering.

**`DD-13` (director, unratified).** Deviations 1–3 are accepted as executed. A sheet that shows a plate its
own group's algorithm did not draw contradicts the text beside it, which is exactly the defect `DD-9` was
written to remove; preferring a same-scheme candidate, and drawing the two corridor groups at the k where
their corridor scheme actually appears, serve `DD-9`'s stated purpose and are disclosed on the sheet itself.
Deviation 3's `COMPLEX_MULTI_WING` correction (reflex-vertex splitting, not morphological opening) is a
factual repair of a sentence that was wrong, in a list the task was already rewriting.

**`FINDING 227`.** Deviation 4 stands as a real, unfixed gap: the `refuse` field on sheets 07, 10 and 11
still states refusal conditions the cutter does not have — no tip-wedge-below-6 m² test, no 15 m arm-length
refusal, no `buffer(−4.0)` opening. The document therefore promises three refusals it will never issue.
Out of this plan's scope; a follow-up task, not a defect of T05.

**Verdict: CP-3 passed. Plan `eu21-t07` is closed.** Deliverables: the wing piece cap and its convex
fallback (T01 + `DD-11`), the junction rectangle withdrawn on its own evidence (T02 / `DD-10` /
`FINDING 225`), the corridor band ending on the cut (T03), the 550-plate re-measure at 440 / 54 / 56
(T04 / `DD-12` / `FINDING 226`), and the rules document brought level with the code and redrawn from the
cutter (T05 / `DD-9` / `DD-13`). 🔴 `DD-9`–`DD-13` are director decisions taken under “continuer
jusqu'a la fin” and await the owner's ratification.

**Owner ruling 2026-09-02, verbatim:** *“yes, don't understand, if improves yes, ok, ok”* — read against the five decisions in order: **`DD-9` ratified**, **`DD-10` not ruled** (“don't understand” — re-explained, still open), **`DD-11` ratified** on its condition (it does improve: 16 floors recovered against 4 lost, net −12 refusals), **`DD-12` ratified**, **`DD-13` ratified**. Four of the five are now law of the arc; only `DD-10` (the junction-rectangle withdrawal) awaits the owner's word.
