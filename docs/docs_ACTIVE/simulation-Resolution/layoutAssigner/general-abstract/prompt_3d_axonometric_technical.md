# Image-generation prompt — LayoutAssigner 3D axonometric, technically annotated (hybrid)

**Purpose:** fusion of the two other prompts — keeps the realistic 3D axonometric architectural-rendering look, but carries the flowchart's full technical density (formulas, field names, function names, decision branch, limitations) as annotation callouts layered on top, like an engineering-magazine annotated cutaway illustration (e.g. a car-brochure exploded-engine diagram: a realistic 3D render with thin leader-lines and small technical index-card labels pointing at each part). Paste the prompt below into Gemini as-is.

**Tool:** Gemini web · **Style:** realistic 3D axonometric rendering + technical annotation layer (leader lines, callout cards, engraved plaques).

---

## Prompt to paste

Create a single wide realistic 3D axonometric (isometric) architectural illustration in the style of an annotated engineering/technical cutaway diagram — think a premium car-brochure exploded-view illustration or a museum engineering exhibit: photorealistic materials, soft studio lighting, gentle shadows, neutral pale-gray background — but with a full layer of thin precise leader-lines connecting each element to small rectangular technical annotation cards (white card, thin dark border, small clean monospace-style text, like an index card pinned to the illustration). The illustration must read as BOTH a realistic 3D rendering AND a dense technical reference at the same time — do not simplify the labels, include every technical detail listed below verbatim.

Compose it as one continuous horizontal sequence of five connected vignettes, read left to right, each on its own floating ground plane linked by a thin amber ribbon/arrow path, all sharing the same camera angle, lighting, and material palette (soft neutrals + one warm amber accent used consistently for arrows, leader-lines, and engraved plaque text).

**Vignette 1 — input:** an irregular real-world city building footprint (L-shaped or notched polygon slab, mid-rise), on a small urban block, neighboring context buildings faded translucent gray. Leader-line to a technical card reading exactly: "REAL BUILDING RECORD — footprint_area_m², num_floors, archetype_id (from semantic enrichment)".

**Vignette 2 — substitution + scaling:** the footprint replaced by a clean rectangular standardized DOE prototype volume, a faint dashed ghost of the original irregular footprint hovering beneath it, plus a translucent scaling grid with corner arrows. Include a small realistic glass/acrylic diamond-shaped plaque floating above, engraved: "BASELINE FOUND?" with two thin amber ribbon paths leading from it — one solid path continuing forward labeled "YES", one dashed side-branch path dropping to a small dimmed alternate mini-building labeled with a card: "NO → fallback: adaptive auto zoning (single_zone / one_zone_per_floor / perimeter_core) + dq_flag tagged". Near the scaling grid, an engraved technical plaque reads exactly: "S = area_real ÷ area_baseline  ·  planar factor = √S".

**Vignette 3 — multi-zone floor plan:** an architectural cutaway/exploded view — the front facade peeled away to reveal one representative floor's interior zoning: a double-loaded corridor down the middle, rectangular room modules on both sides, rendered in soft pastel zone-fill colors (warm beige units, cool gray corridor), each zone type with a short leader-line label ("apartment unit zone", "corridor zone"). Above it, 3-4 translucent ghost floor slabs stack upward fading in opacity, with a leader-line to a card: "ZONE MULTIPLIER — representative floor × N identical stacked levels". A second technical card nearby reads exactly: "scale_baseline_idf(): geometry vertices × √S (walls, windows, shading) · lights / equipment / people / infiltration / outdoor-air / DHW loads × S · per-area density fields untouched".

**Vignette 4 — weather/location handoff:** the vignette split into two halves — left half a muted cold-toned mini skyline with a snowflake icon (native Buffalo climate), right half a warm sunlit mini skyline silhouette with a sun icon (real target city), a thin amber arrow crossing between them. Technical card between the two halves reads exactly: "purge_baseline_outputs() + patch_location_and_weather(): strips baseline Output:* objects · replaces Site:Location / RunPeriod with the target city's real EPW".

**Vignette 5 — simulation & results:** the finished building volume rendered fully solid and polished, subtly glowing at the window bands, connected by a thin amber arrow to a small stylized gear/chip icon (abstract line-chart bars inside it) and a minimalist floating results card (a small bar chart + single glowing number motif, no real numeric text needed). Technical card beside it reads exactly: "EnergyPlus 23.1 — annual 8,760-hour run · manifest tagged zoning_strategy = layout_assign".

**Base plaque (bottom, spanning the full width, like a museum-exhibit caption rail):** a long thin horizontal plaque resting under all five ground planes, engraved in small clean text: "KNOWN LIMITATIONS — fixed-capacity equipment (transformers, DHW tanks, HVAC coils) not yet scaled  ·  native Buffalo CZ 6A envelope still applied (climate-specific patching not yet built)". Render this plaque in a visually distinct material (matte dark slate) so it clearly reads as a caveat/footnote, separate from the main amber-accented technical annotations above.

Keep every quoted technical string exactly as written above — do not paraphrase, shorten, or invent additional labels. The overall impression should be "a realistic architectural illustration you could actually use as a technical spec sheet" — dense with correct technical text, but still elegant and photorealistic, not a flat diagram.
