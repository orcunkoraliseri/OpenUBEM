"""Validation harnesses for the Input-Imputation arc (M09).

Report, do NOT tune: every metric produced under this package is a reporting
artefact for the user/manager. None of it may ever be fed back into an
imputer setting/threshold/bound (zero-fitted-params, M09 §4).

Owned by T08 (`mask_recover.py`, this package marker); T09 adds the sibling
`eui_impact.py` module.
"""
